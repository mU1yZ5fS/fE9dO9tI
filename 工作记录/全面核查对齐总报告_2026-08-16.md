# 全面核查对齐总报告（2026-08-16）

> 核对基准：`F:\毛的遗产改版逆向工程测试\改版最新完整逆向`（Unity 原作）
> 被审工程：`F:\work\中国-毛的遗产[dh-4591]`（Godot 4.7）
> 方法：只读静态审计（find/grep/python 解析源码、场景、资源、uid、引用），**未运行 Godot/headless**；运行时验证交用户在编辑器执行。
> 六份证据：本文件 + 五路子代理报告（主流程外壳 / 外交主场景 / 内容面板 / 事件结局成就 / 资产）+ 本次独立静态体检。

## 0. 总结论

**尚未全量移植，当前不能算“完整可玩”。** 核心循环（启动→主菜单→新游戏→外交地图→推时间→事件/各面板→存档读档）已闭合且可运行，但存在多处**可触达的内容缺口**与**不可达内容**：
- 事件：580 个事件内容 100% 有实现，但 **63 个事件没有任何触发入口**（10.9%）。
- 外交：232 个动作类型缺 19 个定义；国家面板 128 行 TODO，大量逐国链分支整组无按钮。
- 结局：A+B+C 文案 0 缺失，但批D 接线未完成（Event1 错路由、Event111/112 不切场景、Event18 的 11/12 跳过、1992 抉择界面整体缺失）。
- 成就：Unity 137 调用点/96 唯一编号，Godot 仅 28 调用点/24 唯一编号，缺 72 个。
- 战争：Unity 78 个唯一战争 id（200 槽），Godot 只有 12 个 WarDef .tres（且缺 war_09），地图 37 个交战标记无等价物。
- 音乐：78 首原版曲目 0 移植，专辑目录为空，音乐点唱机不可玩。
- 教程 31 页、开局规则 9 组、语言选择、DLC 检测、帝国界面、决议图标 D0-D35、帝国图标 0_0/1_0 整体缺失。
- 工程健康度：静态层面全绿（见 §4），不是“打不开”，是“内容没搬完”。

## 1. 规模快照

| 维度 | Unity 原作 | Godot 重制 |
|---|---|---|
| 构建场景 | 22 个 .unity | 主流程 19 个 .tscn + 组件场景 |
| C#/GDScript | 1420 .cs（根 714 + 子目录） | 644 .gd（场景 51 + 数据脚本 + 事件效果 533） |
| 事件脚本/资源 | EventN.cs 580 个唯一编号 | .tres 582（数字编号 579 + 非数字 3）；效果脚本 533 |
| 战争定义 | ingamewars 200 槽；`new War()` 169 处、78 唯一 id | WarDef .tres 12 个（0-8,10-12，缺 09） |
| 科技 | 34 | 34（数值逐项一致） |
| 焦点 | 20（7 层） | 20（7 层，图标 7/7） |
| 修正 | 图标 id 0-58；文案 62 项 | .tres mod_00-66 共 67 个；图标 67 编号×2 态 |
| 决议 | 53 def（0-52）；图标 D0-D35 共 36 | 53 def 条件/效果全接线；图标 0 |
| 政治家 | politics[18] + 拼脸 | 初始池 18 + 预备池 16；肖像 43 张 |
| 音乐 | 78 曲（156 wav 含 _d1） | 0（专辑目录空） |
| 成就 | Set 调用 137 处 / 96 唯一编号 | set_achievement 调用 28 处 / 24 唯一编号 |

## 2. 分域结论（红=阻断完整游玩，黄=局部缺口，绿=完成）

### 2.1 主流程外壳（子代理 A）
- 🟢 启动链静态闭合：`project.godot` 主场景→启动加载→主菜单（uid 全部可解析）；新游戏→外交场景；保存/加载/设置/关于/退出均有接线。
- 🟢 存档/读档：5 槽 + 摘要 + 铁人标志（非成就槽强制 iron_man=false），格式改为 .res+meta.json（等价替代）。
- 🟢 设置：难度 0-4、音量 ±1/±10、自动保存、游戏内隐藏难度行 已移植。
- 🔴 语言选择：Start.unity + LanguageScript 未移植，全中文硬编码（按 language==0 裁决可接受，但需用户确认）。
- 🔴 开局规则：GameRules.unity 9 组 gamerules 未移植；`场景/开局弹窗/开局弹窗.tscn` 是死资产（无脚本、无引用）。`world_factory.gd:425-428` 自述跳过。
- 🔴 教程：TutorialScript 31 页未移植，主菜单无入口。
- 🔴 关于界面：仅一行致谢+返回；authores.cs 四段名单/URLscript 未移植。
- 🔴 DLC 检测：CreateScript.CheckDLC 未移植；`world_state.gd:239` 硬编码 dlc=[true,true,true,true,false]。
- 🟡 SteamManager：未移植，成就改本地 user:// 持久化（既定裁决）。
- 🔴 音频：播放器/专辑槽 UI 已建，但 `资产/音频/专辑/` 为空 → 专辑总数 0，音乐不可玩。外部 `F:\work\CustomMusic\CustomMusic\Assets\东欧之声` 有 40 首 ogg，但与 Unity 78 曲名 0 匹配（内容不同源），且未导入工程。

### 2.2 外交主场景（子代理 B + 主审独立计算）
- 🟢 逐国链：Unity CountryScript 中文链 Show 864 槽 = Godot 863 `_show` + 1 `_btn`，数量对齐；抽查 5 国按钮数一致。
- 🟢 主循环：1-4 档速度/暂停/日期显示齐；四种地图着色模式齐；滚轮缩放/右键拖拽/触控齐。
- 🔴 动作类型：Unity 中文 Show 232 个 this_type；Godot `_def_N` 仅 213；差集 19 个 =
  `12,20,21,37,47,69,83,109,112,129,139,140,141,147,1011,1012,1055,1073,10001`。
  关键项：20 解放军增援、12 亲华政变、37 推翻波尔布特、47 关系正常化、69 支持共运、83 扶亲华、139/140/141 非洲三国转亲中、1011/1012 罗马尼亚、1055 圣战、1073 托洛茨基遗产、10001 短剑力量（显示）。
- 🔴 国家面板 128 行 TODO/未移植/未建模：整组无按钮的链区间包括 82-88、62-66、72/73、1028-1031、1047-1049 等；单号未移植 49/50/57/58/68/70/1032/1035/1037/1038/1039/1040/1041/1042/1045/1046/1050/1051/1053/1056/1057/1062/1074-1080 等；未建模字段 dlc[3]、cw、data[65]、prcpower、parts、ingamewars[25/26/53/66]、res495、ev638/ev499/ev535/ev483/ev461/ev458/ev560/ev564/ev659/ev661 等门控使整支按钮不出现。
- 🔴 南半球模式：South.unity 无任何对应场景/脚本；投影切换未实现（Godot 为 3D 球+调色板）。
- 🔴 地图交战标记：Unity at_war_script 37 个实例无等价物。
- 🔴 帝国界面：EmpireSceneScript（领导人列表/insider/修正列表）无对应场景；卫星系统是卫星轨道不是帝国界面。
- 🟡 事件 UI 桥：三态机/6 选项/超时机制齐；无事件历史/日志界面。
- 🟡 1992 抉择：in1992_script 整体缺失（见 §2.4）。

### 2.3 内容面板（子代理 C）
- 🟢 政治+派系：6 类政策按钮、5 派系支持/禁止、选举/演讲、17 个政治家操作按钮齐；18 政治家名单与 Unity Politics_inf_d1 一致。数据层缺口：gamerules、Event712、数字事件 444/503/550/551/682、多党法定多数行。
- 🟢 政治家/干部：18 卡+领袖+8 职位任命齐；43 张肖像、34 个池 .tres 引用 0 缺失。未建模：LeaderAsset/公众形象/社会声望。
- 🟡 经济：12 项预算只接 11 项（data[93] 国际援助节点存在但全 hidden 未接线，`经济.tscn:1270-1325`）。
- 🟢 科研：34/34 科技，money/days/year 逐项一致；滑条改相机滚动（表现层差异）。缺口：modifies[51] 石油行、Event110 解锁门。
- 🟡 战争：列表+8 干预按钮齐；但战争定义 12/78，war_09 .tres 缺失，晚线战争无定义。
- 🟢 结果页：事件场景第三态实现；stamp 图章/滚动为表现层简化。
- 🟡 修正：6 面板+列表+图标可玩；64 停用、60/66 效果未录入、62 意大利结算链未移植。
- 🟡 决议：53/53 条件/效果全接线；但 0 图标、无 req/result tooltip。
- 🟢 焦点：20/20、7 层、图标 7 张基础图 MD5 一致。
- 🟡 帝国数据：Empire/Leader/Insider 数据模型 1:1，但无 EmpireScene 式 UI。
- 🟡 成就 UI：主菜单弹窗可展示 24 项；接线下缺口见 §2.4。

### 2.4 事件/结局/成就（子代理 D + 主审独立计算）
- 🟢 事件内容层：Unity 580 个 EventN.cs 全部有 Godot .tres/.gd 实现；数字差集仅 {19}，且 Event19 已以 `event_five_no`（source_event_number=19）逐字逐值覆盖 → 语义 0 缺。资源引用 0 缺失（ExtResource/SubResource 均完整）。
- 🔴 事件触发不可达 63 个（10.9%）：拉美选举链 126-151（26）、意大利链 291-299（9）、党内人物链 301-310（10）、土/希/法/西/意等 376-385/387/389-395（18）。Unity 由 ReqEventForDLC02 的 next_elections/链式触发；Godot country_data 有 next_election 字段但无扫描代码。
- 🔴 触发条件不忠实 2 处：Event5 缺 res503!=0 与 ev444 守卫（会早触发）；Event54 被改成人造 6 个月延迟（Unity 无此延迟）。
- 🔴 结局路由未完成（台账批D）：
  - Event1 错路由：原 data[35]=5 → Godot queue(1)。
  - Event18 的 69/70 号战争战败结局 11/12：`event_018_war_is_over.gd:95-99` 显式跳过（且注释“ENDINGS 无 11/12”已与批C实现矛盾）。
  - Event111/112 只写 `d[I_ENDING_ROUTE]=6`，全工程无消费者，不切场景。
  - GameState.cs 9 处（=3/11/12）、in1992 4 处（=13/1/2/9）、DiploButtonScript:9515/9520（=10）未接线。
  - 1992 抉择弹窗整体缺失；Godot `_check_endings` 在 year>=1993 直接强制 5/6/7/0，与原作“1992 玩家二选一（继续掌权或进摘要）”流程不一致。
- 🟢 结局 A+B+C：胜利 131 串、轮播 121/136/129、坏结局 40、DLC 373/86/123 全部 0 缺失；UI 翻页/DLC 面板/坏结局分流已实现。
- 🔴 成就：Unity 137 调用点/96 唯一编号；Godot 28 调用点/24 唯一编号；缺 72 个。其中 56 个只在 EndingScript（批D 69 处 TODO 已留注释），16 个在非 EndingScript（Button_Pol_Script:689、Results_text:51/55、TimeScript:5965/691/695/3220/3257-3269、GameState:447/1078/1259/1369/1395/1707/1738/1754）。
- 🟡 过时注释：`game_manager.gd:2691`“事件97未移植”已不成立（event_097 已移植并激活 modifier 11）；`world_factory` 若干“未建模”注释亦需复核。

### 2.5 资产（子代理 E）
- 🔴 音乐 0/78；Godot 只有 click_default/click_mouse_over 两个 UI 音 + 东方红.MP3（卫星 jingle，非原曲同源）。Unity Total.all 音效槽本身为空 → 音效无欠账。
- 🔴 决议图标 D0-D35 共 36 张全缺（决议界面纯文字）。
- 🔴 帝国图标 0_0/1_0 缺。
- 🟢 修正图标 0-66 全覆盖 + 2 special，0_0/58_1 与 Unity MD5 一致；仅 Soyuz_s_oligarkhiey×2 未拷（Unity 代码 0 引用）。
- 🟢 焦点图标 7/7，MD5 一致。
- 🟢 字体：Godot 方正跃进简体.ttf 与 Unity 三个 ttf 同 MD5（同源）。
- 🟢 地图：自绘 shader + map_color.png + 3 json，确认不依赖 Unity 贴图。
- 🟢 政治家立绘：43 张、池引用 0 缺失（Unity 无编号立绘集，Godot 为补全）。
- 🟡 puppeticons 傀儡国图标未按原样移植，由 外交/在某国影响下_*.png 等价替代。
- 🟢 引用完整性：tscn/tres 静态引用 3502 条仅 1 条在 android 生成物目录缺失；.import 0 孤儿。

## 3. 静态健康体检（主审独立执行）

| 检查 | 结果 |
|---|---|
| autoload 4 个 uid 解析 | ✅ 全部指向存在文件（音频总管是 .tscn 场景 autoload） |
| 启动链 UID/路径 | ✅ bydan4iqthbaa=主菜单、vq6jexkk5tru=外交等全部存在 |
| 全工程 res:// 引用（.gd/.tscn/.tres） | 4181 条，仅 9 条为动态格式串/测试文件/android 模板，无真实缺文件 |
| class_name | 47 个，0 重复 |
| .uid 文件 | 641 个，0 重复 uid |
| .tscn uid | 23 个，0 重复 |
| UTF-8 严格解码 | 0 错误 |
| 括号配平（启发式去字符串/注释） | 0 异常 |

## 4. 修复优先级建议（分步迭代，每步可回滚）

1. **批D 收口**（已有台账与 TODO 注释，工作量最小、收益最大）：
   - Event1 路由 1→5；Event18 接 11/12；Event111/112 接 queue(6)；GameState/in1992/DiploButton 的 3/10/11/12/1/2/9/13 接线。
   - 1992 抉择弹窗：按 in1992_script.cs 新建（左=继续掌权+铁人标记，右=按条件 13/1/2/9/0 进摘要）；`_check_endings` 的 year>=1993 强制结局需与用户确认是保留还是改回原作流程。
   - 结局批D 69 处 TODO 成就接线。
2. **事件触发 63 个不可达**：补 ReqEventForDLC02 的 next_elections 扫描与链式触发（126-151、291-299、301-310、376-395）。
3. **外交差集**：先补 19 个缺失 `_def`（优先 20/12/37/47/69/83），再按国家面板 128 行 TODO 逐组接线（建议从中国周边 9/38/49/69/70/129 等国家组开始）。
4. **战争**：补 war_09 与其余 73 个动态战争 WarDef（按 Unity `new War()` 调用点生成），把 world 槽位从 34 扩到 200；补 37 个地图交战标记。
5. **音乐**：把可用音源（CustomMusic 40 首 ogg 或用户指定曲库）导入 `资产/音频/专辑/<专辑名>/`，验证专辑槽/单曲槽。
6. **外壳**：教程 31 页、开局规则 9 组（含 iron_man 判定与美苏焦点路线）、语言选择/DLC 检测（按用户裁决是否做）、关于页名单与 URL。
7. **资产**：决议图标 D0-D35、帝国图标 0_0/1_0；经济 data[93] 项；修正 60/62/66 效果。
8. **编辑器验证清单**（交用户）：新游戏→推 1 年→读档→事件 5/54 触发时机→126-151 链是否出现→1992 抉择→结局 11/12→成就弹窗 24 项→设置音乐页是否空。

## 5. 证据出处索引

- 五路子代理报告（本会话）：主流程外壳 / 外交主场景 / 内容面板 / 事件结局成就 / 资产。
- 既有台账：`工作记录/外交互动对齐_自检报告.txt`、`工作记录/事件626_712对齐台账.md`、`工作记录/结局对齐台账.md`、`工作记录/架构优化方案.md`。
- 关键源行：见各分域括号内行号；外交动作差集可由 `DiploButtonScript.cs`（Show L28-5624 / OnMouseDown L8596-12715）与 `场景/外交界面/外交互动_批1..6.gd` 独立复算。
