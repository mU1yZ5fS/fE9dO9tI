# 结局对齐台账（A+B+C 批次）

## 范围裁决（用户确认，2026-08-16）
- 本轮 A+B+C：本体 EndingScript 全部判定与文案、DLC Ending1-23 判定与文案、结局场景 UI 行为。
- 下批 D：事件触发点 `data[35]` 接线 + `achievements.Set(N)` 成就接线。
- 只做 `PlayerPrefs.GetInt("language")==0` 中文分支；俄文独立分支（OnMouseDown L3127+、BadEnding L3871+）不迁移。
- 不跑 headless；验证=静态自检+读码推演，编辑器运行验证交给用户。

## 权威源码出处
- 本体：`F:\毛的遗产改版逆向工程测试\改版最新完整逆向\Assets\Scripts\EndingScript.cs`
  - GoodEnd L57-995 / OnMouseDown 中文 L1043-3126 / BadEnding 中文 L3695-3870 / 分流 Awake+DoneEnding L6-55 / 翻页 L1025-1042 / Text L4077-4121 / Update L4144-4194
- DLC：`Assets\Scripts\Ending1.cs`~`Ending23.cs`（无 Ending9；只翻译 TextOfEnding，Update 不迁移）
- 场景挂接：`Assets\Ending.unity`（EndingsFirstDLC 8 页 / EndingSecondDLC 7 页 / EndingThirdDLC 7 页）、`SwitchEndingScript.cs`、`EngingDLCController.cs`
- 依赖文案：`Assets\Resources\new_texts_en.xml`、`Events_text_en.txt`（GameStartScript.cs:900-929 按换行 split 加载）

## Godot 产出文件
| 文件 | 内容 |
|---|---|
| `场景/结局界面/结局内容_基础.gd` | NEW_TEXTS（25 条）+ NEW_TEXTS_EXTRA（24 条）+ NEW_EVENTS_TEXT（118 条）+ `_leader_name/_nt/_net` |
| `结局内容_本体_胜利.gd` | `build_good_end(w)` ← GoodEnd 中文条件链（461 行） |
| `结局内容_本体_轮播1.gd` | `try_build_page` 页 1-4 ← L1049-1727 |
| `结局内容_本体_轮播2.gd` | `try_build_page` 页 5-9 ← L1728-2493 |
| `结局内容_本体_轮播3.gd` | `try_build_page` 页 10-18 ← L2494-3126 |
| `结局内容_坏结局.gd` | `build_bad_ending(route,w)` data[35]=1-14 ← L3695-3870 |
| `结局内容_DLC1.gd` | `try_build_dlc(1,page,w)` Ending1-8（page 0-7） |
| `结局内容_DLC2.gd` | `try_build_dlc(2,page,w)` Ending10-16（page 0-6） |
| `结局内容_DLC3.gd` | `try_build_dlc(3,page,w)` Ending17-23（page 0-6） |
| `结局.gd`（重写） | 分流（current_ending_id>0→BadEnding；否则 GoodEnd+轮播）、本体左右翻页 0-17 循环、DLC 三面板切换 + 右翻页 DLC 页循环 8/7/7、Up/Down 键滚动、`|`→换行、Unity 标签→BBCode |
| `结局.tscn` | 文案 Label→RichTextLabel（bbcode/autowrap=3/scroll_active）、左右翻页按钮、DLC 面板四按钮 |

## 逐字一致性验证（源码字面量 vs GDScript，允许 70 字符拆段合并；2026-08-16 终验）
- 胜利 131 串、轮播1 121、轮播2 136、轮播3 129、坏结局 40：全部 0 缺失
- DLC1 373 串、DLC2 86 串、DLC3 123 串：全部 0 缺失（`\u00a0` 按运行时等价解码比对）
- 词法：10 个文件 tab 缩进/引号闭合/括号平衡全过；主控代码括号 71/71、3/3、17/17
- 接口签名：8 个内容文件全部与主控 preload 调用一致
- 基础层字典：NEW_TEXTS/NEW_TEXTS_EXTRA/NEW_EVENTS_TEXT 共 159 个 key，EndingScript+Ending1-23 静态与动态引用全集覆盖，0 缺失
- tscn 终态：RichTextLabel(bbcode/autowrap=3/scroll_active/normal_font_size=28)、标题 50 号加宽、左右翻页(flip_h 复用右翻页纹理)、DLC 面板四按钮

## 主控修复记录（子代理交付后的主控级修正）
- 轮播2：19 处依赖空串 → `_nt/_net`（含 `string.Format` 格式串无占位符、多余参数被忽略的语义注释）
- 轮播3：9 处 `new_texts` 空串 → `_nt`；胜利：`new_events_text[536]` → `_net(536)`
- DLC2：Ending15 `new_texts[281-286]` 六分支 → `_nt`
- DLC3：L513 C# 残留语法错误修复；`NTE_*` 82 个重复常量统一替换为 `_net()` 并删除；动态下标 1487-1489/1542-1564 共 22 条补录基础层
- 主控：DLC 模式右翻页承担 EngingDLCController.OnMouseDown 翻页语义（8/7/7 循环）

## 编辑器报错修复记录（2026-08-16，用户 LSP 日志）
- 空 if/elif/else 块补 pass：坏结局 3 处、胜利 22 处、轮播3 13 处（共 38 处；原版仅成就 TODO 注释导致空块，批D 接线后移除）
- Python 风格 `"...".format([...])` 非法：胜利 17 处改为基础层新增 `_fmt(fmt, args)`（等价 C# string.Format 的 {0}/{1} 复用语义）
- 主控：`const DLC_页数 := [0, 8, 7, 7]` 补回；8 处 preload 脚本非静态调用全部改 `.new()` 实例调用
- 删除 `tmp/b2`、`tmp/ending_b1` 子代理模板残留（Godot 扫描 res://tmp 报错）
- 复核：旧日志中「does not exist / W not declared / 轮播1 L42 / DLC2 L398 / DLC3 L132」均为子代理中间版本报错，当前文件对应位置正常；终态静态检查 10 文件全过，本体 557 串逐字零缺失

## 批D 待办清单（下批）
- 触发点 `data[35]` 赋值接线（参考源 grep 清单）：
  - Event1:451(=5)、Event4:194(=2)、Event5:317(=1)、Event7:147 / Event8:136(=3)、Event18:143/151(=11/12)、Event25:183(=2)、Event94:362(=1)、Event111:162 / Event112:151,164(=6)、Event303:66(=7)、Event581:136(=13)、Event666:42(=8)、Event692:37(=14)
  - GameState.cs 9 处（=3/11/12）、in1992_script.cs 4 处（=1/2/9/13）、DiploButtonScript.cs:9515/9520(=10)
- 成就 `achievements.Set(N)` 接线：GoodEnd 22 处 + OnMouseDown 63 处 + BadEnding 2 处（各文件内已留 `TODO(结局批D)` 注释，编号与源码行号齐备）
- 触发点接线后移除 事件效果 中「ENDINGS 表仅 0-7，暂跳过」的差异注释（如 event_018_war_is_over.gd L95-99）
