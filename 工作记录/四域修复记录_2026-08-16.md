# 四域修复记录（2026-08-16 第二轮）

> 范围：事件 / 外交 / 结局 / 战争。其余域不修。
> 不跑 headless；验证交用户在编辑器执行。

## 1. 结局
- `event_018_war_is_over.gd`：拆分 69/70 战争判定并接 `queue_ending_after_event(11/12)`（Event18.cs:143/151）。
- `event_001_elections.gd`：结局路由 1→5（Event1.cs:451）。
- `event_111/112`：接 `queue_ending_after_event(6)`。
- `外交互动_批2.gd` type49：两处 `GameManager.trigger_ending(10)`（DiploButtonScript.cs:9515/9520）。
- 新增 `event_ending_choice.tres/.gd`：1986-01-01 终局抉择（TimeScript.cs:683 + in1992_script.cs 左右按钮）。
- `game_manager._check_endings` 删除无源 1993 强制结局/预算/派系自动结局，仅保留人口崩盘 ToEnding(4)。

## 2. 事件
- `event_005_popular_discontent.tres`：补原版 `(!ev444 || res444!=0) && res503!=0` 守卫。
- `event_054_reform_investment.tres` + `game_manager`：删除人造 6 个月延迟，恢复 `ev45 && data[16]>11`。
- 新增 `数据脚本/req_event_triggers.gd`：63 个死链事件触发器（RequrementsDLC02/03/01 对应分支，行号注释齐），接入 `tick()`。

## 3. 外交
- 新增 `场景/外交界面/外交互动_补遗.gd`：19 个缺失动作定义（12/20/21/37/47/69/83/109/112/129/139/140/141/147/1011/1012/1055/1073/10001）。
- `外交互动目录.gd` 接入补遗 → Unity 中文 Show 232 / Godot _def 232，差集 0。
- 澄清：生产链 `国家面板_逐国链.gd` 已是 864/864 完整；旧 `国家面板.gd` 128 行 TODO 为过时对照链。

## 4. 战争
- 新增 `资产/数据/战争/war_09、war_36-48`（含 TimeScript.cs 硬编码漂移）+ 其余 60 个战争定义，WarCatalog 86 id。
- `war_system.apply_war_drift_extra`：补 war40(ev563)/war41(ev565) 条件漂移。
- `world_factory._init_wars`：战争槽 34 → 200（GameState.cs:7824）。
- `war_system.apply_war_result`：70/71/72/73/74/75/90 按 950 阈值路由失败结局 12/11/12/11/3/11/11。

## 5. 成就
- 结局批D：67 处 TODO 全部接线；清理 38 个过时 pass 占位。
- 非结局：Button_Pol 24、TimeScript 28/62/63/131/155-158 已接。
- 中文分支剩余 7 个（25/27/54/123/133/143/151）依赖 Results_text 的 data[111/112] 与 GameState.WarResult 各战争胜利分支，待该批移植时同步接线。
- 39/66/160 属俄文分支，按既定裁决不迁。

## 6. 解析 Bug 修复（2026-08-16 用户报错后）
- `外交互动_补遗.gd`：删除局部 lambda `di()` 直接调用，改为类方法 `_data_at()`；
  `build_action` 移到全部 `_def_*` 之后；助手函数统一放在调用者之前。
- `game_manager.gd`：同类局部 lambda 调用已改为 `_dv()` 类方法。
- 新增静态检查 `tmp/_check_helper_calls.py`（`_xxx(` 调用必须在本文件/基类/GameManager 找到定义）。
- 全工程扫描：无任何“局部 lambda 变量被直接调用”的写法。

## 7. 战争专属结算与胜利特效（GameState.WarResult）
- 5/19/23/25/26/30/31/32 专属阈值结算已移植（含第二阶段重启：war5 阿富汗、war30 西班牙）。
- 70/71/72/73/74/75/90 胜利特效已移植（不含 text 文案，项目惯例不显示 WarResult 文案；
  ILoveSuckCocks 地图 parts 刷新按项目惯例近似省略）。
- 绑定成就 54/123/133/143/151 已接线。
- 成就 25/27：原版 data[111]/data[112] 仅开局归零（GameStartScript.cs:854-855），
  全工程无任何累加点 → 原版即死条件，按“死代码不迁移”惯例不接。中文分支成就实际全量。

## 8. 剩余待办（四域内，等用户裁决）
- 战争 70-75/90 胜利文案：项目目前无 WarResult 文案展示位；如需逐字文案需新做结果文本 UI。

## 9. 解析错误修复（用户报错：war_system.gd Could not preload）
- 根因：`_apply_war_result_defeat` 末尾残留一段没有 `match` 头的孤儿 case 块
  （`6:` / `70,71,...90:` / `return false`），已删除。
- 新增全工程“孤儿数字 case”扫描：当前 0 处。
- 再次确认：全部静态检查通过（括号/UTF-8/class_name/uid/助手定义）。

## 10. Reload 警告清理（用户编辑器日志）
- `war_system.gd`：`restarted` 改名 `achievement_restarted` 消除 CONFUSABLE_LOCAL_DECLARATION；
  `_war30_result` 未用参数改 `_d`；删除未用的 flag31/flag32。
- `外交互动_补遗.gd`：整数除法加 `@warning_ignore("integer_division")`；
  局部 `war` 改名 `war_data` 消除 SHADOWED_VARIABLE_BASE_CLASS。
- 事件脚本批量清理：36 个 `prepare()` 未用 `world` 参数改为 `_world`；
  4 个遮蔽基类 `d` 的局部变量加 `@warning_ignore("shadowed_variable_base_class")`；
  `event_674` 未用 `trade` 参数改为 `_trade`。





## 11. 剩余玩法缺口核查修复（第一批）
- ① 政治体制重算：补齐 TimeScript.cs:1333-1360 的 DevelopedConsumerism、
  modifies[40]/[38]、事件 502（含政党制度8再-3）、681、674/675 条件。
- ② 双周块：`_fortnight_trait_influence`（TraitInfluence 主体+中央职务块）与
  `_fortnight_mutual_relations`（MutualRelationsChange 完整，dlc[0] 恒 true 分支）
  已接入 _on_fortnight 原版行序位置。
  ⚠ 外交部职位块（politics_dolshnost[2]，TimeScript.cs:11696 起）仍为显式 TODO stub。
- ④ 外交链：completedDecisions[14]/[6] 两处 flag 近似改为 w.decisions.completed 真实字段；
  其余 6 处（relres/guns/war_state/Israellost）沿用项目既有约定。
- ③⑤⑥ 待后续批次。

## 12. 第二轮核查修复
- ③ ModifiesChanges：18-42 号修正周期效果已移植（ModifiesInfuence.cs:1866-2260），
  43-65 待续；60/62/66 效果待续。
- ④ completedDecisions[14]/[6] 两处近似已改为真实 decisions 字段。
- ② TraitInfluence 外交部职位块仍 TODO；MutualRelationsChange 已完整接入。

## 13. 第三轮核查修复
- ③ ModifiesChanges 全部周期效果 0-65 已移植（18-42 上轮，本轮 42-51/53/58/59/61/63/65；
  含 51 石油消费公式、65 MoneyLevel/LeaderProperty 全套）。
- ③ 修正 60/62/66 核查结论：Unity 无 modifies[66]；60 仅图标显示无周期效果；
  62 仅开局激活+图标显示，Godot 已按意大利结算分近似展示 → 无实际缺失。
- ④ 剩余 6 处近似（relres/guns/war_state/Israellost）确认为项目既有建模约定，不动。

## 14. 第四轮核查修复（TraitInfluence 全量闭环）
- ② 外交部职位块（politics_dolshnost[2]，TimeScript.cs:11648-12186）已整合进
  `_trait_influence_foreign`；并修复一处机械翻译语义错误：
  原版 `party_ideology[k] += (Σideology)/D`，旧产物误翻成 `+= D`（除数当增量），
  新增 `_ideology_total`/`_add_ideology_share` 恢复动态份额语义，13 处调用全部改接。
- ② 总理块（TimeScript.cs:12187-12658，302 条语句）与主席块
  （TimeScript.cs:12659-13178，344 条语句）逐字移植为 `_trait_influence_premier`/
  `_trait_influence_chairman`；调用点由 if/elif 链改为与原版一致的 4 个独立 if
  （中央/外交/总理/主席）。修正翻译器 `relations--/++` 误补右括号问题。
- ② 发现并修复中央职务块缺口：原 `_trait_influence_central` 只有 traits[3]/[1]/[2]，
  漏掉了 TimeScript.cs:11235-11296 的 traits[0]（含 data[56] 分支）段，本轮补齐。
- 验证工具化：`tmp/_verify_trait_chief.py` 对外交/总理/主席/中央个性段做 C#↔GD
  逐语句差分（957 条全部 0 差异）；`tmp/_verify_trait_central.py` 对中央 traits[3]/[1]/[2]
  做 match/case 结构树差分（0 差异，含 case29 边界守卫与三元等价的归一）。
- 全部静态检查通过：括号平衡 0、UTF-8 0、class_name/uid 0、助手调用 0 缺失。
- ⑤ 战争胜利文案展示位、⑥ DLC 检测仍维持上轮结论（项目无展示位/按既有裁决）。

## 15. 第五轮核查修复（战争 WarResult 全分支补齐）
- 按 GameState.cs `data[82]==N` 分支补齐战争结算：0/1/2/4/6、42/43/46、48~52、
  62、64~69、76~80、82~89；原有 70~75/90 走 950 阈值路线。
- war42 伊拉克内战、war48 苏丹内战、war50/51 肯尼亚/索马里、war62 刚果革命、
  war68 安哥拉多阵营、war85 马里-布基纳法索等复杂分支均按 Unity 中文效果端口。
- 新增 `_apply_war0/1/2/4/6/42/43/46/48~52/62/64~69/76~80/82~89_result`。
- 补齐 `资产/数据/战争/` 占位名：war14~16、18~32 填入中文名；新建 war_20.tres（乍得战争）。
- 全工程无 `TODO/待办/未建模/未移植/未接入` 标记。
