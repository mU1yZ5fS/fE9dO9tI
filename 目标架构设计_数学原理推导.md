# 目标架构设计 — 从数学原理推导「数据 · 逻辑 · UI」最优形态

> 适用对象：中国:毛的遗产[Godot重制版]（单机、确定性回合/双周结算、事件驱动政治模拟，GDScript/Godot 4.7）。
> 方法：把架构选择表述为**带约束的多目标优化问题**，用五个数学透镜（图论 / 信息论 / 复杂度 / 关系理论 / 控制论）逐项推导，每个设计决策给出原理依据与现状落差。
> 结论先行：最优形态 = **内容数据化 + 领域纯逻辑 + 服务编排 + 视图模型 UI**，依赖图为分层 DAG，状态为单一权威具名模型，每 tick 计算从 O(全部事件) 降到 O(活跃事件)。

---

## 0. 问题形式化：这是带约束的优化，不是风格偏好

设架构 A 的代价函数：

```
J(A) = α·C_coupling + β·R_redundancy + γ·T_tick + δ·M_maintenance
```

约束（本项目的硬约束，任何解必须满足）：
- **C1 行为保真**：与原版（及当前已对齐行为）逐事件一致——规则语义不能因重构漂移；
- **C2 存档兼容**：旧存档可加载；
- **C3 平台性能**：GDScript 解释执行，长局（30+ 游戏年、每双周结算）不卡顿；
- **C4 单机确定性**：无多人、无外部服务，状态演化可复现。

最优架构不是「某一种风格」，而是上述问题在约束下的**帕累托前沿**。下文每个决策都指出它优化了哪个代价项、受哪个约束约束。

---

## 1. 五个数学透镜

### 1.1 图论：依赖结构必须是无环分层 DAG

把「谁引用谁」建成有向图 G=(V,E)。三个定理级结论：

- **可独立演化性**：G 的强连通分量（SCC）中，>1 节点的分量意味着两个模块互相依赖——无法单独测试、单独替换、单独理解。目标：SCC 全部为单点。
- **稳定依赖原则（Martin）**：每个包有三个指标——`Ca`（被依赖数，Fan-in）、`Ce`（依赖数，Fan-out）、不稳定度 `I = Ce/(Ca+Ce)`、抽象度 `A`（抽象类型占比）。主序列距离 `D = |A + I − 1|`，D 越大越「不稳定却被人依赖」或「稳定却没人依赖」。**被大量依赖的包必须抽象（A 高），否则任何修改都炸一片。**
- **分层定理**：若存在分层 L1→L2→…→Ln（依赖只指向下层），则依赖图自动无环，SCC 全单点，且上层可独立替换。**分层的数学功能就是把「任意图」约束成「拓扑序」。**

现状落差点：`GameManager` 的 `Ca ≈ 884`（全项目引用），`Ce` 也很大（它引用地图/战争/科技/政客/存档一切），`A≈0`（纯具体实现）→ D 极大 → **最不稳定、又最被依赖**，是图论意义上的最坏位置。同时 `数据脚本` 层内大量互引 + 事件脚本直接引用 autoload → 出现大 SCC（事件脚本→GameManager→事件引擎→事件脚本 的环）。

### 1.2 信息论：单一权威源（SSOT），冗余是可量化的负资产

设状态真实值 X，存储位置集合 {S_i}。若同一信息存放在 X 与 Y 两处且要求一致，则：

- 互信息 I(X;Y) 刻画「重复信息量」，而同步成本 = 所有写入点必须同时写两处（写入点数量 × 不一致风险）。
- 正确做法：**X 是唯一存储，Y := f(X) 是纯函数派生**。此时 I(X;Y) 为确定性函数关系（无自由冗余），同步成本 = 0，不一致概率 = 0。
- 同理，**内容（语义数据）与代码（行为）分离**是信源编码：同一段语义（如"苏联影响力≥500 时可用该外交动作"）只应出现一次。出现两次（一次在 .tres、一次在 _def_N 代码）就是冗余编码，改一处漏一处。

现状落差点：`I_USA_RELATIONS` 与 `empires[0].relations` 双轨（写入点分布在 `_add_empire_relation`、事件脚本、镜像同步函数多处）；外交动作语义散落在 232 个 `_def_N` 代码函数；同一 helper 逻辑 253~287 份副本——信息论意义上全是冗余编码。

### 1.3 复杂度理论：把「每 tick 全量」换成「事件驱动 + 索引」

设事件数 E=583，每条件求值成本 c。当前触发逻辑每 tick：

```
T_tick = O(E · c)   —— 全部事件全量求值
```

事件触发本质上是**稀疏的**：任一时刻满足触发条件的事件数 K 远小于 E。若建立「按条件类型分桶的候选索引」（如：flag 类条件→哈希到涉及 flag 的桶；日期类→时间堆；资源阈值类→脏标记），则：

```
T_tick = O(K + 维护索引的增量成本)
```

K 通常是个位数。长局中 E 只增不减（内容扩展），索引化使成本与内容规模解耦。同样的原理适用于 UI 刷新：**状态变更通知（推送）优于每帧轮询全量状态（拉取）**，后者是 O(状态量×帧率)，前者是 O(变更量)。

### 1.4 关系理论：状态模型必须过规范化检验

把 `数值表[200]` 当作一张表来审：

| 规范化检验 | 数值表[200] 现状 | 结论 |
|---|---|---|
| 1NF（无重复组） | 200 个同构整数槽 = 经典重复组（repeating group），语义藏在注释里 | **违反** |
| 候选键 | 键是魔法数字下标，无声明、无类型 | **无键** |
| 2NF/3NF（无部分/传递依赖） | `I_USA_RELATIONS`（data[28]）依赖 `empires[0].relations` = 传递依赖；`I_INCOME` 等大量「可由其他字段计算」的存储 = 派生冗余 | **违反** |

修法即规范化：具名字段（1NF + 声明键）→ 派生值一律**计算属性**而非存储（3NF）。注意：计算属性的每次调用成本是 O(1)~O(k)，以 CPU 换一致性，数学上总是划算（GDScript 属性调用 ≈ 几个整数运算）。

### 1.5 控制论：游戏 = 确定性状态机，存档 = 快照 + 迁移

游戏是确定性状态机 M = (S, Σ, δ, s₀)：状态 S（领域数据）、输入 Σ（玩家决策 + 时间推进）、转移 δ（规则结算）。

- **不变式**：每条规则本质是「状态满足前置条件 → 转移后满足后置条件 + 钳制」。把钳制（0~1000 关系、非负预算）集中到**状态层的唯一写入通道**（而不是散落在 253 个副本里，有的钳有的不钳），不变式就从"期望"变成"保证"。
- **存档**：快照序列化（当前做法，正确）。事件溯源（日志重放）在本项目**不是最优**：没有审计/多人需求，日志重放成本高于快照，故保留快照 + **版本号 + 迁移链**（migrator vN→vN+1），取代现在的散落 `migrate_legacy_*` 补丁。

---

## 2. 现状量化诊断（五透镜下的体检表）

| 透镜 | 度量 | 现状值 | 目标值 |
|---|---|---|---|
| 图论 | GameManager 不稳定度 D | 极大（Ca≈884, A≈0） | 服务层每个服务 D→小 |
| 图论 | SCC 规模 | 事件脚本↔GameManager↔EventEngine 成环 | 全单点 |
| 信息论 | 语义冗余 | helper 副本 253~287 份；镜像槽 ≥2 处；外交动作语义代码化 232 个 | 每语义 1 处 |
| 复杂度 | 每 tick 触发 | O(583·c) 全量扫描 | O(K)，K=活跃事件数 |
| 规范化 | 状态表 | 反 1NF、无键、3NF 违反 | 具名 1NF、派生计算化 |
| 控制论 | 写入通道 | 分散（事件脚本直接 `d[i] += x` 无钳制统一） | 唯一写入通道 + 不变式 |

---

## 3. 目标架构（推导结果）

### 3.1 分层总图与依赖规则

```
┌─────────────────────────────────────────────────────────┐
│  UI 层（表现）    场景/*.tscn + 界面脚本                  │
│   - ViewModel（纯 RefCounted）：订阅状态 → 计算展示数据    │
│   - 组件化：状态栏 ×1 复用、卡片预制件                    │
│   - 输入 → 调用应用层命令；绝不直接写规则                 │
└──────────────┬──────────────────────────────────────────┘
               ↓ 只依赖服务接口（构造注入，禁全局引用）
┌──────────────┴──────────────────────────────────────────┐
│  应用层（编排）  服务：TimeService / EventService /       │
│   DiplomacyService / EconomyService / WarService /      │
│   SaveService / SettingsService / AchievementService    │
│   - 每个服务是 Godot Node，无 UI 引用                     │
│   - 服务间通信走信号总线（GameEvents）或显式接口          │
└──────────────┬──────────────────────────────────────────┘
               ↓ 只依赖领域层
┌──────────────┴──────────────────────────────────────────┐
│  领域层（纯逻辑，无 Node、无 autoload、无场景引用）        │
│   DomainState（Resource，具名字段，SSOT，存档根）          │
│   Rules：ConditionEvaluator / EffectExecutor /           │
│          FortnightSimulator / PolicyEngine / EconomyCalc │
│   - 全部纯函数：输入 (state, ctx) → 输出 (state', 事件)    │
└──────────────┬──────────────────────────────────────────┘
               ↓ 只依赖数据层
┌──────────────┴──────────────────────────────────────────┐
│  数据层（内容）  .tres/JSON/CSV 资源                       │
│   EventDef / DecisionDef / FocusDef / WarDef /           │
│   DiploActionDef / CountryChainDef / ModifierDef / 文案    │
│   - 内容不在代码；生成器负责产出                          │
└─────────────────────────────────────────────────────────┘
```

依赖规则（图论推导的直接结论）：
1. 依赖只允许向下；任何 `res://场景/` 出现在领域/应用层 = 违规（当前仅 2 处，保持为 0）。
2. **禁全局服务定位**：`GameManager.xxx` 一律改为构造注入 / context 传入。数学理由：注入使依赖边**显式**，Ca/Ce 可度量，SCC 可消除；服务定位使依赖边隐藏，图分析失效。
3. 领域层连 Godot 引擎都尽量少依赖（只用 RefCounted/Resource/Array 等基础类型）——这是可测性的数学前提：纯函数集合可以在无场景树环境下穷尽测试。

### 3.2 数据层设计（内容 = 编码后的语义，唯一出现一次）

| 内容 | 载体 | 关键点 |
|---|---|---|
| 事件/决策/焦点/战争 | .tres（保留现有） | 效果 DSL 补全声明式能力，CUSTOM_SCRIPT 从 90% 降到 <20% |
| 外交动作 | **新增 DiploActionDef**（conditions: ExprNode[] + effects: EffectNode[] + caption 模板） | 232 个 `_def_N` 全部迁出代码；条件/效果复用现有 DSL |
| 逐国链 | CountryChainDef（国家 × 槽位 × 条件/效果） | 2,336 行代码 → 表 |
| 世界初始数据 | JSON/CSV（world_factory 内嵌 const 迁出） | 数据不参与编译 |
| 文案 | CSV → Godot translation（已有通道） | TXT_ 常量清零 |
| modifier 规则 | ModifierDef 表（部分已有 .tres） | 结算按表驱动 |

DSL 补全方向（保持序列化稳定）：给 EffectNode 增加**通用原子效果**（改字段、钳制、触发、条件嵌套），事件专属复杂行为保留脚本钩子但走**白名单接口**（见 3.3）。

### 3.3 领域层设计（本架构的心脏）

**DomainState（1NF + SSOT）**：
- 具名字段：`people_support: int`、`relations_usa: int`… 每个字段一个声明位置。
- 兼容层：保留 `数值表` 数组但标记 deprecated，`get/set` 转发到具名字段（旧存档 → migration 一次性搬移）；新代码禁止写数组。
- 派生值全部计算属性：`income`、`usa_relations` 等不再存储，`func 收入() -> int` 现算（3NF）。
- **唯一写入通道**：所有变更经 `apply(变更集)`（钳制、不变式检查、变更通知 emit 集中在此）——控制论结论的直接落地。

**规则引擎（纯函数）**：
- `ConditionEvaluator.eval(node, ctx) -> bool`：无副作用，可单测，支持属性测试（随机状态 × 断言）。
- `EffectExecutor.exec(effects, ctx) -> 变更集`：返回**变更集**而非直接改状态——变更集可审计、可测试、可合并（一次 tick 批量应用）。
- `FortnightSimulator.step(state, 输入) -> (state', 变更集)`：结算从 GameManager 的 3,000+ 行 `_fortnight_*` 迁入，按 ModifierDef 表驱动分派。
- 事件脚本钩子：`execute(ctx)` 保留，但 ctx 注入 `ws`/`变更通道`，**禁 `GameManager.`**（884 处耦合的数学修复：把隐式全局边改成显式参数边）。

### 3.4 应用层设计（编排，不演算）

- GameManager 瘦身为 Orchestrator：`tick()` = 收集输入 → 调 EventService.check() → 调 FortnightSimulator.step() → 广播变更 → 检查结局/成就。194 个函数中的计算类全部迁出，只剩流程。
- EventService：**触发索引**落地处——维护「活跃候选集」（按 flag 哈希桶 + 日期堆 + 资源脏标记），`check()` 只扫候选集（复杂度 O(K)）。
- 服务间用信号总线：`state_changed(field, old, new)`、`day_advanced(date)`、`event_triggered(id)`…（事件触发已示范过，推广到全部）。

### 3.5 UI 层设计（表现，不承载规则）

- **ViewModel 模式**：每个界面一个 VM（RefCounted），持有 DomainState 引用 + 订阅信号，输出「展示用字典」；Control 脚本只做绑定（VM 数据 → 控件）与转发输入。数学理由：UI 复杂度与状态量解耦，刷新从「每帧全量拉取」变「变更推送」（1.3 的复杂度原理）。
- 状态栏：**一份组件**（子场景 + 参数化），六个界面复用（消除 6 份副本——信息论冗余归零）。
- 巨型场景（派系 1,792 行）拆成卡片级子场景复用。
- 规则展示（可用性检查、条件文本）由领域层提供**查询函数**，UI 只显示，不重算。

### 3.6 横切设计

- **存档**：`DomainState` 快照 + `format_version` + 迁移链 `migrate(vN) → vN+1`；运行时瞬态（pending 事件、链队列）独立导出（现有模式保留）。C2 约束的正式化。
- **性能预算**：每 tick 总成本 ≈ O(活跃事件) + O(结算表行数) + O(变更通知)；启动已有异步化，保留。
- **确定性**：禁止随机数散落（现有 `randf` 集中评估）；C4 约束下存档可复现。

---

## 4. 关键决策的数学论证表（为什么这是最优）

| # | 决策 | 优化项 | 原理 | 不做它的代价 |
|---|---|---|---|---|
| 1 | 状态具名化 + 唯一写入通道 | β 冗余、M 维护 | 1NF/SSOT；不变式集中 | 每次改字段全局搜索 645 文件 |
| 2 | 依赖注入替代服务定位 | α 耦合、可测性 | SCC 消除；Ca/Ce 显式 | 依赖图不可分析，改 GameManager 炸一片 |
| 3 | 内容出代码（外交/逐国链/文案） | β 冗余、M 维护 | 信源编码：语义唯一出现 | 改一个动作要改代码、重编译、不可调 |
| 4 | 效果返回变更集 | 可测性、C1 保真 | 纯函数可穷尽测试；变更可审计 | 规则漂移无法回归检测 |
| 5 | 触发索引化 | γ 每 tick 成本 | 稀疏性：O(K) vs O(E) | 内容越多越卡，长局掉帧 |
| 6 | ViewModel + 信号推送 | γ 帧成本、α | 推送 vs 轮询；观察者模式 | UI 每帧全量刷新，状态一多就卡 |
| 7 | 快照 + 迁移链（不做事件溯源） | M 维护 | 无审计需求时日志是纯成本 | 迁移补丁散落，旧档破裂 |
| 8 | 状态栏/卡片组件化 | β 冗余 | 复用 = 单点修改 | 6 份副本 6 处改 |

约束检查：C1（行为保真）——领域层保留原版语义注释与对照，迁移以回归基线（冒烟 + 快照比对）把关；C2（存档）——迁移链 + 兼容读写层；C3（性能）——索引 + 推送 + 变更集合并；C4（确定性）——纯函数 + 集中随机。

---

## 5. 迁移路径（约束下逐步逼近，每步行为不变）

目标架构不是一次性重写（违反 C1），而是**逐边替换依赖图**：

1. **安全网**：headless 冒烟 + `evaluate/step` 纯函数单测 + 存档快照比对（先让"行为不变"可检验）。
2. **去冗余**：删 253~287 份 helper 副本（先裁决钳制分叉）→ 信息论 β 下降。
3. **断全局边**：事件脚本 `GameManager.` → context 注入（884 条边改为显式参数）→ 图论 SCC 消除。
4. **抽服务**：按 3.4 拆出 8 个服务，GameManager 只剩编排 → α 下降。
5. **具名化**：DomainState 门面迁移（旧数组读写转发 + migrator）→ 规范化完成。
6. **内容出代码**：外交/逐国链/文案/初始数据迁入数据层 → β 归零。
7. **索引化 + VM**：触发候选集、ViewModel 状态栏 → γ 达标。

每步独立可回滚；第 3~5 步之间顺序可调，但 1 必须先于一切。

---

## 6. 边界：这套架构何时「不是」最优

- 若目标是**尽快上线不再迭代**：重构全不划算，直接砍 B~F 只留安全网——J(A) 的权重变了，最优解随之改变。
- 若未来要**多人/模组热加载**：数据层要加 mod 加载器与哈希校验，领域层要加权威-客户端划分（本设计未覆盖）。
- 若内容量再增长 10 倍（数千事件）：`.tres` 文本资源要考虑编译期合并或二进制资源（启动加载已异步化，可承受）。
- 若性能预算极度紧张（低端 Android）：事件脚本钩子全部改数据化，脚本钩子白名单收缩到 <5%。

以上边界说明本设计是针对**当前约束集**的帕累托最优，而非放之四海皆准的教条。
