# 魔法数字重构清单与可行性研究

> 适用范围：当前 Godot/GDScript 项目（尤其 `数据脚本/services/*` 与事件脚本）。
> 状态：P0 进行中。已完成：`fortnight_simulator.gd` 裸 `_dv` / `_addi` 下标（577 处）、缺失 `I_*` 常量、裸帝国下标（214 处）、trait `match` 裸 ID（52 处）、全项目 `mod_active` / `_mod_active` 裸 modifier ID（632 处）、事件编号 `event_result` / `event_done` / `_event_result` 等裸 ID（约 36 处）。已通过 Godot 启动验证（无脚本解析错误）。

---

## 1. 当前严重程度

扫描范围：全项目 667 个 `.gd` 文件（已剔除注释与字符串内容）。

| 指标 | 数量 |
|---|---:|
| 数字字面量总数 | 62,194 |
| 出现在 `const` / `enum` 定义行上的数字 | 527 |
| 其余散落在普通代码/数据表中的数字 | 61,667 |

你贴出的 7 个服务文件合计约 5,082 个数字字面量：

| 文件 | 数字字面量 | 主要问题 |
|---|---:|---|
| `fortnight_simulator.gd` | 4,301 | 裸数据下标、trait ID、modifier ID、阈值 |
| `policy_service.gd` | 433 | 政策切换费用、路线限制、阈值 |
| `faction_service.gd` | 275 | 政策 ID、事件 ID、modifier ID |
| `settings_service.gd` | 36 | 默认值/范围常量 |
| `economy_service.gd` | 29 | 经济公式中的倍数和阈值 |
| `save_service.gd` | 4 | 铁人模式哨兵值 |
| `game_service.gd` | 4 | 默认哨兵值 |

**结论：魔法数字滥用依然严重，且存在“已有常量却不用”的情况。**

---

## 2. 重构能改多大：量化评估

### 2.1 可机械替换的裸下标/ID

| 类别 | 数量 | 说明 |
|---|---:|---|
| `fortnight_simulator.gd` 中 `_dv(d, N)` / `_addi(d, N, ...)` / `_add_empire_relation(w, N, ...)` | 791 处 | 其中 778 处已有 `W.I_*` 常量可直接替换 |
| 全项目 `_dv` / `_addi` 裸数字调用 | 592 处 | 主要集中在 `fortnight_simulator.gd`（577 处） |
| `get_country_by_legacy_index(N)` 裸下标 | 1,529 处 | 现有 `GameConstants.LegacySlot` 只覆盖约 30%（456 处） |
| `_add_empire_relation(w, N)` 裸帝国下标 | 271 处 | 基本是 0/1，可统一为 `Empire.USA / Empire.USSR` |
| `mod_active` / `_mod_active` 裸 modifier ID | 155 处 | 可统一为 `GameConstants.Modifier.*` |
| `set_achievement(N)` 裸成就 ID | 112 处 | 可统一为 `GameConstants.Achievement.*` |
| `event_result` / `event_done` 裸事件 ID | 30 处 | 可统一为 `GameConstants.EventId.*` |

### 2.2 语义化改造量

| 类别 | 数量 | 说明 |
|---|---:|---|
| `econ_system` 数值比较/判断 | 591 处 | 可用 `is_planned_economy()` / `economy_orientation()` 等语义函数替代 |
| `_change_politicians({...})` 裸 trait 键 | 25 处 | 键是 `trait_personality` 裸数字，可改用 `GameConstants.PoliticianPersonality.*` |
| `_change_politicians_at_least / _above` 裸参数 | 4 处 | 可改为枚举参数 |

### 2.3 工作量估计

- **P0（机械替换）**：约 2,900+ 处，主要是把已有常量名套上去，风险低，但量大。
- **P1（补常量表）**：`LegacySlot` 从 8 个扩充到覆盖 150+ 个国家下标；`EventId`、`Modifier`、`Achievement`、`PolicyId` 等新增常量表。
- **P2（语义化 API）**：把 `econ_system` 判断、trait 修改封装成可读函数/枚举，事件脚本可读性提升最大，但需要先定边界。

> 估算不是“全部 6.2 万数字都要改”。数据表（地图、国家、事件文案）里的数字大多属于数据而非魔法数字；真正值得优先处理的是逻辑里的裸下标、ID 和阈值，约在数千处规模。

---

## 3. 直观化设计：让事件脚本“说人话”

### 3.1 政治家特质：从裸数字到语义枚举

当前事件里常见写法：

```gdscript
# 现在：字典键是 trait_personality 的裸数字
_change_politicians({
	2: [-100, 0],   # 2 是什么？改革派？
	0: [100, 0],    # 0 是什么？极左派？
})

# 或直接赋值
politician.trait_personality = GameConstants.PoliticianPersonality.MODERATE
```

建议改成：

```gdscript
# 方式 A：直接用现有枚举，键名可读
_change_politicians({
	GameConstants.PoliticianPersonality.REFORMIST: [-100, 0],
	GameConstants.PoliticianPersonality.FAR_LEFT: [100, 0],
})

# 方式 B：再加一层“事件语义 API”，更接近自然语言
change_politician_traits([
	{ "trait": PoliticianTrait.FAR_LEFT, "loyalty": 100, "power": 0 },
	{ "trait": PoliticianTrait.REFORMIST, "loyalty": -100, "power": 0 },
])

# 方式 C：如果只是“把某个政治家的特质改成极左派”
set_politician_trait(politician, PoliticianTrait.FAR_LEFT)
```

其中 `PoliticianTrait` 可以是 `GameConstants.PoliticianPersonality` 的别名或再封装一层：

```gdscript
enum PoliticianTrait {
	FAR_LEFT = GameConstants.PoliticianPersonality.FAR_LEFT,
	MODERATE = GameConstants.PoliticianPersonality.MODERATE,
	REFORMIST = GameConstants.PoliticianPersonality.REFORMIST,
	LIBERAL = GameConstants.PoliticianPersonality.LIBERAL,
	CONSERVATIVE = GameConstants.PoliticianPersonality.CONSERVATIVE,
}
```

### 3.2 政体/经济体制偏向：从区间比较到语义函数

当前代码里大量出现：

```gdscript
if d.econ_system <= 11:
	# 计划
elif d.econ_system > 13:
	# 市场
elif d.econ_system >= 13 and d.econ_system <= 14:
	# 混合/过渡
```

建议先定义枚举和判断函数：

```gdscript
enum EconomyOrientation {
	PLANNED,   # 偏计划
	MIXED,     # 混合/过渡
	MARKET,    # 偏市场
}

## 按当前项目/原版规则：
## 计划：econ_system <= 11
## 混合：12 <= econ_system <= 13
## 市场：econ_system >= 14
func economy_orientation(econ_system: int) -> EconomyOrientation:
	if econ_system <= 11:
		return EconomyOrientation.PLANNED
	if econ_system <= 13:
		return EconomyOrientation.MIXED
	return EconomyOrientation.MARKET

func is_planned_economy(econ_system: int) -> bool:
	return economy_orientation(econ_system) == EconomyOrientation.PLANNED

func is_market_economy(econ_system: int) -> bool:
	return economy_orientation(econ_system) == EconomyOrientation.MARKET
```

事件脚本就可以写成：

```gdscript
if is_planned_economy(d.econ_system):
	# 计划经济事件
elif is_market_economy(d.econ_system):
	# 市场经济事件
```

如果不想传参，也可以在 `WorldState` 上加便捷方法：

```gdscript
func is_planned_economy() -> bool:
	return economy_orientation(econ_system) == EconomyOrientation.PLANNED
```

这样“某个范围内的经济体制代表偏计划，其他的偏市场”就从一堆 `<=11 / >13` 变成了直观的语义判断。

---

## 4. 分文件重构清单

### P0：`fortnight_simulator.gd`（最高优先）

1. `_dv(d, N)` / `_addi(d, N, ...)` 裸下标替换为 `W.I_*`。
2. 无现成常量的下标补充到 `world_state.gd`：113-116、131、143、153、157、160、161、185。
3. trait 的 `match` 分支改用 `GameConstants.PoliticianBackground / Alignment / Special / Personality`。
4. modifier ID 改用 `GameConstants.Modifier.*`。
5. 帝国下标改用 `Empire.USA / Empire.USSR`。
6. 国家 legacy 下标改用 `GameConstants.LegacySlot.*`。
7. 成就 ID 改用 `GameConstants.Achievement.*`。
8. 事件 ID 改用 `GameConstants.EventId.*`。
9. 平衡阈值提取为文件级 `const`。

### P1：`policy_service.gd`

1. `diff * 50`、`diff * 300` 提取为常量。
2. `> 66`、`>= 700` 提取为常量。
3. 政策 ID 集合 `[9,14,15,22,23,28,29]` 提取为常量数组。
4. 事件 ID `444` 改用 `GameConstants.EventId.*`。
5. `world.factions[0]` 改用 `FactionData.MAOIST`。
6. 派系重排数值（50/200/400/500/700 等）提取常量。

### P2：`faction_service.gd`

1. modifier ID 改用 `GameConstants.Modifier.*`。
2. 事件 ID（444/503/550/551/682）改用 `GameConstants.EventId.*`。
3. 决策 ID（13/16/18）新增 `GameConstants.DecisionId` 或文件级常量。
4. `DOCTR_BASE` / `DOCTR_MOD6` 保留为数据表，但 key 尽量使用 `PolicyId` 常量。

### P3：`economy_service.gd`

提取常量：

```gdscript
const PLANNED_ECON_MAX := 12
const PLANNED_ECON_PENALTY_DIVISOR := 10
const BUDGET_PLANKA_DIVISOR := 6
const DEFAULT_BUDGET_STEP := 10
const ENVELOPE_CUT_PARTY_MULTIPLIER := 4
const LOAN_LIMIT_DIVISOR := 5
const REPAY_PARTY_SUPPORT_GAIN := 5
const REPAY_INFLUENCE_MIN_AMOUNT := 10
const DIFFICULTY_HARD := 3
const DIFFICULTY_NORMAL := 2
```

### P4：`settings_service.gd`

集中定义默认值和范围常量，例如：

```gdscript
const VOICE_MIN := 0
const VOICE_MAX := 100
const VOICE_DEFAULT := 5
const AUTOSAVE_MODE_DEFAULT := 0
const SAVE_PLACE_MIN := 1
const SAVE_PLACE_MAX := 5
const SAVE_PLACE_DEFAULT := 5
const ACHIEVEMENT_SLOT := 5
const DIFFICULTY_MIN := 0
const DIFFICULTY_MAX := 4
const DIFFICULTY_DEFAULT := 2
```

### P5：`save_service.gd` / `game_service.gd`

- `iron_override` 的 `-1/0/1` 改为枚举或常量。
- 默认参数里的 `-1` 改为 `NO_INFLUENCE` / `NO_SIDE`。

---

## 5. 建议新增/扩充常量表

| 常量组 | 位置 | 内容 |
|---|---|---|
| `EventId` | `GameConstants` | 444、503、550、551、682 等 |
| `Modifier` | `GameConstants` | 0-65 的 modifier 语义名 |
| `Achievement` | `GameConstants` | 28、131、155-158 等 |
| `Empire` | `GameConstants` | USA=0、USSR=1 |
| `PolicyId` / `DecisionId` | `GameConstants` | 政策 ID、决策 ID |
| `LegacySlot` 扩充 | `GameConstants` | 覆盖全项目使用到的国家 legacy 下标 |
| 缺失 `I_*` | `world_state.gd` | 113-116、131、143、153、157、160、161、185 |
| `EconomyOrientation` | `GameConstants` 或 `WorldState` | PLANNED / MIXED / MARKET |
| `PoliticianTrait` 便捷枚举 | `GameConstants` | 可读的事件/API 层枚举 |
| 服务内平衡值 | 各服务文件顶部 `const` | 50、300、66、700、6、4、5 等 |

---

## 6. 验证清单

1. 替换后确认以下模式不再出现：
   ```bash
   grep -nE "_dv\(d[[:space:]]*,[[:space:]]*[0-9]+" 数据脚本/services/fortnight_simulator.gd
   grep -nE "_addi\(d[[:space:]]*,[[:space:]]*[0-9]+" 数据脚本/services/fortnight_simulator.gd
   ```
2. 打开 Godot 项目，确认无脚本解析错误。
3. 跑一次游戏，重点检查：
   - 政策切换费用/党支持扣除
   - 双周结算后数值
   - 设置保存/读取
   - 存档铁人模式
4. 对事件脚本做抽样回归，尤其是 `_change_politicians` 和 `econ_system` 判断相关事件。

---

## 7. 优先级建议

1. 先做 P0：`fortnight_simulator.gd` 裸下标替换，投入产出比最高。
2. 再补 `LegacySlot` / `EventId` / `Modifier` / `Achievement` 常量表。
3. 然后做 `EconomyOrientation` 语义函数，替换 591 处经济体制判断。
4. 最后把 `_change_politicians` 等事件辅助函数改成可读 API。

---

## 8. 是否还有必要继续：阶段性判断

### 已完成的高价值机械替换

| 类别 | 数量 |
|---|---:|
| `_dv` / `_addi` 数据下标 | 577 |
| 帝国下标 | 214 |
| trait `match` 裸 ID | 52 |
| modifier ID | 632 |
| 事件编号（event_result / event_done / _event_result） | 约 36 |

这些替换风险低、收益明确，已经基本把“最扎眼”的裸下标/ID 清掉了。

### 剩余主要魔法数字

| 类别 | 数量 | 继续做的价值 |
|---|---:|---|
| `get_country_by_legacy_index(N)` 国家下标 | 1,531 | 数量巨大，但大量是数据/事件参数；补 150+ 国家常量收益递减，容易引入大规模 diff |
| `set_achievement(N)` 成就 ID | 112 | 可做，但很多成就只有 `ACH_n` 占位名，语义命名收益有限 |
| `econ_system` 数值判断 | 591 | **价值最高**：直接关系事件可读性，建议做 `EconomyOrientation` 语义函数 |
| `_change_politicians({...})` 裸 trait 键 | 25 | 数量少但事件作者最常接触，建议做可读 API |
| 各服务内的费用/阈值 | 数十处 | 建议顺手做，风险低 |

### 建议

- **不要再无差别继续“数字 → 常量”的机械替换**。尤其是国家 legacy 下标和全部成就 ID，改动量很大，但可读性提升有限，还容易让 diff 失控。
- **值得继续的是语义化重构**：
  1. `EconomyOrientation` + `is_planned_economy()` / `is_market_economy()`
  2. `_change_politicians` 改用 `GameConstants.PoliticianPersonality.*`
  3. 事件链/决议触发的具意 ID 输出（已有 `tools/print_event_chain_tree.py`）
- 如果目标是“让事件脚本写起来像人话”，继续做语义 API 比继续堆常量更有价值。
