# 架构独立评审与重构方向 — 中国:毛的遗产[Godot重制版]

> 本报告完全基于对当前代码库的静态勘察（文件规模、继承关系、耦合引用、重复代码、数据流），不依赖任何既有文档。
> 勘察基线：645 个 .gd 脚本 / 约 12.77 万行（wc 两批合计 105,730 + 21,957）/ 27 个场景 / 583 个事件 .tres / 4 个 autoload。

---

## 0. 架构全景（事实层）

```
[autoload 层]
  GameManager   (7,528 行, 194 个函数)   ← 时间/存档/设置/地图/预算/政策/科技/事件/结局/两周结算
  EventEngine   (1,073 行)                ← 事件注册表 + 每 tick 全量扫描 + 条件/效果求值器
  Achievements  (约 400 行)               ← 1000 位图忠实复刻原作
  音频总管       (约 350 行)               ← 音乐点唱机

[数据/逻辑层 数据脚本/]
  world_state.gd        (780)  存档根对象：数值表[200] + countries/factions/empires/wars/politicians
  world_factory.gd    (1,641)  初始数据全部内嵌 const
  war_system.gd       (5,232)  全 static func 静态类（145 处 func）
  politician_system.gd  (579)
  decision_atoms/catalog (1,786/894), modifier_catalog (1,476)
  事件效果/            534 个脚本（每事件一个，全部继承 event_script_base）

[UI 层 场景/]
  27 个 .tscn（派系 1,792 行、科研 1,653、外交 1,618 等巨型场景）
  国家面板.gd (2,674) + 国家面板_逐国链.gd (2,336)
  外交互动_批1~6 + 补遗 (7 个文件共约 9,400 行)
  6 份状态栏.gd 重复脚本；概览.gd (883)、政治.gd (1,000)、派系.gd (744)、世界地图渲染.gd (734)
```

**运行时主循环**：GameManager.tick() → 每 tick 让 EventEngine.check_and_fire() 全量扫描 583 个事件 → 命中 → .tres 选项 → 效果 90% 以上走 CUSTOM_SCRIPT → `new()` 事件脚本实例 → `execute(context)` 直接读写 `GameManager.world`。

---

## 1. 核心架构问题（按根因排序）

### 问题 1（根因）：数据模型是「平坦整型数组」，不是领域模型

`WorldState.数值表[200]` 是唯一权威数据源。所有系统——UI、事件、外交、战争、经济——都以「数组下标」为公共接口：

- 访问依赖三层映射：`d[W.I_PEOPLE_SUPPORT]`（常量）→ `数值索引` 字符串字典（"people_support"/"民众支持" 等别名）→ 数组下标。类型系统完全无法表达领域概念，改名/加字段 = 全项目搜索。
- **双轨镜像仍在扩散**：`I_USA_RELATIONS`（data[28]）注释明写「权威在 empires[0].relations」——同一语义两个存储位置，同步逻辑分散在 `_sync_*` 各处。
- **魔法数字未清零**：事件脚本中 `d[数字]` 直接索引仍有 **632 处**（vs 使用 `W.I_` 常量的 4,769 处）。`_fortnight_modifiers` 等 656 行大函数里遍布裸索引。

后果：任何「语义重构」（如把民众支持拆成对象字段）都牵动 645 个文件；这也是下面所有问题的放大器。

### 问题 2：GameManager 是上帝对象，且全项目以「服务定位器」模式硬依赖它

- 7,528 行 / 194 函数。职责至少 10 组：时间与速度、存档/设置/快捷键、地图数据线程预加载、预算/贷款/储备、政策检查链、科技、事件调度、结局、**3,000+ 行两周结算规则**（`_fortnight_*` 40+ 个函数、`_apply_modifier*` 系列）、政变/成就。
- 全项目 **884 处 `GameManager.` 直接引用**，最大消费者是数据层本身：war_system 189、event_engine 48、politician_system 16；事件脚本在 `execute()` 里直接调 `GameManager.kill_politician()`。
- 信号解耦只做了一半：只有事件触发走 `event_triggered` 信号，其余全部直接调用。
- 后果：数据层无法脱离 autoload 单测/复用；依赖是隐式的（脚本一上来就 `GameManager.world`）；任何改动都可能影响全部 645 个文件。

### 问题 3：事件系统「DSL 被架空」，脚本即内容

- 事件定义已资源化（.tres + EventDef/ExprNode/EffectNode DSL），但效果层 **90%+ 走 CUSTOM_SCRIPT 逃生舱**：event_003 注释自述「因『覆写政治家』『仅 [1..4] 忠诚』不可声明式 → 全部走自定义脚本」。534 个事件脚本成为事实上的内容载体。
- 每 tick 对 583 个事件全量 `evaluate()`（45 种条件类型 match），每次效果执行 `custom_script.new()` 新实例——无索引、无复用。
- DSL 自身也向内容妥协：`expr_node.gd` 为单个事件链新增枚举（`EMPIRE_LEADER_IS`、`SOCIALIST_COUNT_AT_LEAST`…），且「新类型必须追加在组合节点之后：旧 .tres 按枚举整数序列化」——序列化格式脆弱，已开始阻碍演进。

### 问题 4：「按移植批次切分」被当成了架构

- 外交系统按原版移植工单切成 `外交互动_批1~6 + 补遗`（7 个文件约 9,400 行），**每个文件自带一个独立 `build_action()` 分发器**；运行时 `DiploActionCatalog` 把 7 个批次各 `new()` 一遍后线性扫描找动作。
- 232+ 个外交动作是手写 `_def_N` 函数（数字命名）；`外交互动_基础.gd` 提供 30+ 个单字母微型访问器（`d()`/`ev()`/`res()`/`fl()`…）——「C# 逐字转写」的可读性代价。
- `国家面板_逐国链.gd`（2,336 行）按国家手写编号逻辑（`_china_numbers`/`_soviet_numbers`/`_sev_satellite_numbers`…）。
- `国家面板.gd`（2,674 行）把 UI 刷新、动作构建、逐国逻辑混在一个文件，且自标「[已弃用]」「不再走生产路径」的死代码仍保留（约 2,000+ 行）。

### 问题 5：「基类收敛」未完成，重复代码带行为分叉

- 530/645（82%）脚本继承 `event_script_base.gd`，但收敛只做了一半：事件脚本仍各自私有一份 helper 副本——`_add` ×287、`_enable` ×285、`_disable` ×277、`_add_relation` ×253、`_add_power` ×231、`_leave_alliances` ×57、`_join_alliances` ×17（均为「重定义」计数）。
- **这些副本与基类行为不一致**：如 `event_027_032_diplomacy.gd` 的 `_add_relation` 无 0~1000 钳制，基类有钳制——同名函数、不同语义，是调试陷阱。

### 问题 6：UI 层「UI 即规则」+ 大规模复制粘贴

- 6 份 `状态栏.gd`（事件/外交/战争/概览/派系 + 概览界面内）各自重写同一套状态显示逻辑，无人继承共用的 `GameUIBase`。
- `概览.gd` 用 `_build_trade()`/`_build_influence()` 等函数手工拼 RichTextLabel 长文本报告。
- 巨型场景：派系.tscn 1,792 行、科研 1,653 行——节点树不可组件化复用。
- 中英混杂标识符贯穿全项目（`数值表`、`音频总管`、`曲目变更`、`内战中`、`政变中`、`有驻军基地`、`派系颜色`），合法但严重损害可维护性与协作。

### 问题 7：工程健康

- **零自动化测试**，无 CI；唯一插件是 `copy_all_errors`（复制报错）。
- 工作区残留 `概览.gd.bak` 等备份文件。
- 存档兼容靠散落的 `migrate_legacy_*`（数值表 0.1.x 迁移、战争超时迁移等）逐案打补丁，无统一版本化迁移层。
- 事件双标识：`event_id` 与 `source_event_number`（原版数字）并行，存档 `completed_event_ids` 混用两种 key。

### 做得好的地方（重构应保留的资产）

1. 事件/决策/焦点已资源化（.tres）且有生成器（event_factory），内容层有数据化的正确方向。
2. 启动优化已落地：事件异步扫描（64 批量 + 每帧限量收尾）+ 地图线程预加载，说明团队具备性能意识。
3. `WarSystem` 全 static + 显式传参 `(w, d)`——依赖显式化，是比直接访问 autoload 更好的样板。
4. 存档以 WorldState 为根对象，运行时状态（pending 事件、链队列）有独立的 export/import 通道。
5. 数据层基本不反向依赖 UI（数据脚本引用 res://场景/ 仅 2 处）。
6. `W.I_*` 索引常量已收敛 4,769 处引用，命名有 `数值索引` 别名映射。
7. git 提交规范（feat/fix/chore 前缀）。

---

## 2. 重构方向（按优先级与风险排序）

### 阶段 A：回归安全网（任何重构之前，阻塞项）
- 写一个 headless 冒烟测试：启动 → new_game → 快进 N 个 tick → 导出关键状态快照（数值表关键项、事件完成位图、战争状态）。
- 为 EventEngine.evaluate / execute、GameManager 的 `_fortnight_*` 纯计算函数（签名多为 `(d, w)`，天然可测）建立单元测试（GUT 或最小自研 runner）。
- 存档兼容测试：旧档加载后断言关键值，作为数值表改造的护栏。

### 阶段 B：纯删除与去重（零/低行为风险，先做）
1. 删除事件脚本 253~287 份私有 helper 副本——**先逐函数裁决行为分叉**（如 `_add_relation` 钳制差异：按基类钳制版本统一，需人工确认原版语义）。
2. 删除 `国家面板.gd` 自标 [已弃用]/不再走生产路径的死代码；删除 `.bak` 文件。
3. 事件脚本的 `GameManager.world` / `GameManager.xxx()` 调用改为从 `context` 注入（`execute(context)` 已传 context，加 `ws` 与必要的服务句柄即可）——这一步把 884 处耦合砍掉大半，且是纯机械替换。

### 阶段 C：按「结算边界」拆 GameManager（不按行数）
- 先拆出：`FortnightSimulator`（`_fortnight_*` + `_apply_modifier*`，3,000+ 行）、`EconomyService`（预算/贷款/储备）、`PolicyService`、`MapDataService`（线程预加载）、`SaveService`、`SettingsService`。
- 拆法参照 WarSystem 样板：static func + 显式传参，先同文件搬家再拆文件，保持 GameManager 只留「编排」。
- `_fortnight_modifiers`（656 行）按 modifier 类型拆成数据表驱动的分派。

### 阶段 D：数据模型演进（根因，最长线）
- 采用「门面 + 分批搬迁」：WorldState 保持 `数值表` 字段与旧索引读写（存档兼容），新增具名字段（`@export var people_support: int`），新代码写字段、旧代码走转发；逐步把 632 处魔法数字清零。
- 合并镜像双轨（relations 单一权威），删除 `I_USA_RELATIONS` 等镜像槽或改为只读视图。
- 长期目标：`数值表[200]` 退化为存档兼容层，领域数据全部具名。

### 阶段 E：内容出代码
- 外交 `_def_N`（232+ 个）→ 数据表/Resource，条件与效果复用 ExprNode/EffectNode DSL；`DiploActionCatalog` 的 7 实例线性扫描改为按编号哈希直查。
- 逐国链（2,336 行）→ 数据驱动（国家 × 槽位 × 条件/效果）。
- 事件脚本内大段 `const TXT_` 文案 → CSV/translation（项目已有 `本地化.zh.translation` 与 text_library 通道）。
- `world_factory.gd`（1,641 行）内嵌 const 初始数据 → JSON/CSV 外部数据。
- 6 份状态栏 → 统一组件（继承 GameUIBase 或抽成可复用子场景）。

### 阶段 F：事件引擎性能与结构
- 触发索引化：按 flag/日期/完成位图分桶「活跃事件集」，替代每 tick 583 全量 evaluate。
- 事件脚本实例池或把 execute 改为无状态（context 传入全部依赖），避免每次效果 `new()`。
- 停止向 ExprNode 枚举追加单事件类型；新表达力走「数据 + 通用脚本钩子」。

---

## 3. 优先级速览

| 优先级 | 动作 | 收益 | 风险 |
|---|---|---|---|
| 1 | 阶段 A 回归基线（冒烟 + 单测） | 后续所有重构的前提 | 无 |
| 2 | 阶段 B 删重复/死代码 + context 注入 | −约 1.5 万行、884 处耦合砍半 | 低（先裁决分叉） |
| 3 | 阶段 C 拆 GameManager（先拆两周结算） | 最大文件瘦身、职责清晰 | 中 |
| 4 | 阶段 D 数值表具名化（门面式） | 根因缓解，后续全受益 | 中（需存档护栏） |
| 5 | 阶段 E 内容出代码 | 内容可配置、可调、可本地化 | 中 |
| 6 | 阶段 F 事件引擎索引化 | 长局性能 | 低 |

**明确不要做的事**：继续按「批」切分文件；为单事件往 ExprNode 加枚举；在 UI 脚本里新增规则逻辑；把数值表推倒重来（无回归基线时）。
