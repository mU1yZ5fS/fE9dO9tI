# 钢4化（内容开放）重构路线评估 — 基于最新代码状态

> 评估时点：UI 组件化/服务拆分/数据 JSON 化已提交（commit 5143799）。
> 目标：玩家拿到游戏后，通过编辑游戏目录内的文件即可修改/新增 国家、事件、科技、政治家、决议、外交动作、战争、修正。

---

## 1. 当前地基盘点（已完成 vs 剩余）

### ✅ 已完成（近期提交）

| 项 | 状态 | 说明 |
|---|---|---|
| 数据脚本分层 | ✅ | `core/`（数据类）/ `factory/`（构建器）/ `services/`（经济/政策/结算/存档/设置服务）/ `systems/`（引擎）/ `ui/`（ViewModel）/ `外交互动/` / `地图数据/` |
| GameManager 瘦身 | ✅ 进行中 | 已注入 SaveService/EconomyService/FortnightSimulator/PolicyService |
| 国家数据出代码 | ✅ | `world_factory` 改读 `资产/数据/初始/country_rows.json`（+south/names） |
| 战争目录化 | ✅ | `war_catalog` 扫描 `资产/数据/战争/*.tres` |
| 政治家池 | ✅ | `politician_pool` 管理初始/预备池（.tres 数据） |
| 地图数据化 | ✅ | `资产/地图/generated/map_data.res` + `map_actors/map_neighbors.json`，`build_map` 工具链 |
| 状态栏 ViewModel | ✅ | `ui/status_bar_view_model` 已接入 状态数值区 |
| UI 组件化 | ✅ | 状态栏/导航栏/科技条目/外交 8 子场景 |

### ❌ 剩余关键缺口（钢4化的真正工作量）

| 缺口 | 现状 | 钢4化影响 |
|---|---|---|
| **效果层去脚本化** | 583 事件中 581 含 CUSTOM_SCRIPT（99.7%），脚本效果 1808 次 | 玩家写事件必须写 GDScript → **最大拦路虎** |
| 事件导入器 | 无 JSON/文本 → EventDef 的导入路径 | 玩家事件格式 |
| 决策数据化 | `decision_catalog` 仍代码内嵌（_build_v1/v2） | 玩家改决议 |
| 科技数据化 | `tech_state` 仍 const 数组 + 科研.gd const 文案 + 条目 UI 未数据驱动 | 玩家改科技 |
| 外交动作 loader 接线 | `diplo_action_loader.gd` **已建好但未接入**（国家面板仍走旧 DiploActionCatalog） | 外交动作开放 |
| **数据外置/加载管线** | 全部数据在 PCK 内，无玩家可编辑目录 | **玩家"改本体"的前提** |

---

## 2. 钢4化的四条支柱与对应工程

```
P0 数据外置 + 加载管线（ModLoader）     ← 地基，1~2 天
P1 事件：效果去脚本化 + JSON 导入器      ← 最大工程，2~3 周
P2 决策/科技数据化                       ← 1~2 周
P3 外交动作 loader 接线 + 国家扩展       ← 2~3 天
P4 玩家文档 + 示例 mod + 校验器          ← 收尾
```

### P0：ModLoader（数据外置层）

```
游戏目录/
├── 游戏.exe / .pck        ← 代码 + 场景 + 内置默认数据
└── data/                  ← 首次启动自动生成，玩家直接编辑
    ├── events/*.json  decisions/*.json  techs/*.json
    ├── politicians/*.tres  wars/*.tres  modifiers/*.tres
    ├── countries/*.json  diplo_actions/*.json
    └── mods/xxx/          ← 可选：多 mod 共存（同 id 覆盖，后启者胜）
```

- 加载管线（autoload `ModLoader`）：扫描 data/ → 解析 → **注册表合并**（EventEngine 事件表、DecisionCatalog、TechCatalog、PoliticianPool、WarCatalog、ModifierCatalog、国家表、外交动作表）
- 覆盖策略：data/ 覆盖内置；mod 间按启用顺序后者胜
- 容错：单文件损坏 → 收集错误日志 + 启动界面提示，**回退内置数据，游戏不崩**
- 存档兼容：按 id 存取（现状已如此），卸载内容后未知 id 忽略

### P1：事件开放（最大工程 = 效果层去脚本化）

现状 99.7% 事件效果走脚本。**先补声明式效果原子，再写 JSON 导入器**：

1. **效果原子补全**（把脚本高频操作声明式化）：
   - 政治家操作：忠诚增减 / 入职 / 移除 / 覆写属性（event_003 类）
   - 逐国/逐人循环：对标签列表批量施加
   - 条件效果块：if/elif 嵌套
   - 动态文案钩子：{0}{1} 占位替换、按条件选文案（现有 prepare() 钩子体系保留）
2. **JSON 导入器**：`{"type":"add_resource","key":"budget","value":-10}` → ExprNode/EffectNode 树 → EventDef（对照 event_factory 反向实现）
3. **既有 581 个脚本事件**：保持 CUSTOM_SCRIPT 兼容（脚本作为"高级效果"保留，导入器支持 `{"type":"custom_script"}`），**不需要逐个迁移**——玩家新事件用声明式，旧事件不动，行为零漂移
   - 这是与上一版评估的关键修正：**去脚本化不是迁移 581 个事件，而是"补原子效果 + 新事件走声明式"**，风险大降

### P2：决策/科技数据化

- 决策：`decision_catalog._build_v1/v2` 代码 → 生成 `decisions/*.json`（DecisionDef 字段已齐，把 build 代码的每条转 JSON，load 替代 build）
- 科技：`tech_state` const（依赖/花费/年份）→ `techs/*.json`；`科研.gd` 文案 const → JSON；**条目 UI 数据驱动**（条目位置/类别从数据读——关系线已自动，条目位置数据化后科技树可整体重排）
- 科技 UI 的"36 实例 + 位置"可由数据生成（保留编辑器可见的组件，位置改为数据覆盖）

### P3：外交动作与国家

- **接线 `diplo_action_loader`**：国家面板改走 loader（数据驱动），`_def_N` 代码保留为内置数据源（或逐步迁出）——2~3 天
- 国家：已有 country_rows.json 外置 ✓ 玩家可直接改数值；完整新国家（含地图）依赖 build_map 工具链文档化

### P4：交付物

- 示例 mod 包（改一个国家数值 + 加 3 个事件 + 1 个政治家）
- 玩家文档（data/README.md 自动生成：格式说明 + 字段表 + 校验错误对照）
- 校验器：加载期报错信息友好化（"events/xxx.json 第 12 行: 未知效果类型 add_resurce，是否想写 add_resource?"）

---

## 3. 依赖顺序与工作量汇总

| 阶段 | 内容 | 前置 | 工作量 |
|---|---|---|---|
| P0 | ModLoader（目录约定/扫描/覆盖/校验/回退） | 无 | 1~2 天 |
| P1a | 效果原子补全（政治家/循环/条件块/动态文案） | P0 | 1 周 |
| P1b | 事件 JSON 导入器 + 示例 | P0 | 3~5 天 |
| P2a | 决策 build 代码 → JSON | P0 | 3~5 天 |
| P2b | 科技 const → JSON + 条目 UI 数据驱动 | P0 | 1 周 |
| P3 | 外交动作 loader 接线 | P0 | 2~3 天 |
| P4 | 文档/示例/校验器 | P1-P3 | 3~5 天 |

**总计约 4~5 周**。P0 之后玩家即可改：国家数值、政治家、战争、修正、外交动作（接线的）；P1 完成后事件开放（玩家 mod 的最常见需求）。

## 4. 风险（更新版）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 效果原子补全的表达力缺口 | 中 | 保留 custom_script 高级钩子兜底；补全后抽样覆盖统计 |
| 581 个既有事件行为漂移 | **低**（修正） | **不迁移旧事件**，仅新增声明式路径，零漂移 |
| 存档与 mod 内容卸载 | 低 | id 前缀规范 + 未知 id 宽容（现状已如此） |
| 科技条目位置数据化后布局回归 | 低 | 数据默认值 = 现状坐标，无数据时回退 |
| 玩家文档质量 | 中 | 示例 mod + 校验器错误提示 + data/README 自动生成 |

## 5. 结论

钢4化不是"能不能"，而是**已走过 60%**：数据分层、国家/战争/政治家/地图数据化、服务拆分、外交动作 loader 都已就位。剩余路径清晰且可并行：

1. **P0 ModLoader（1~2 天）**——立刻让玩家可改国家/政治家/战争/修正（数据外置 = "改本体"）
2. **P1 事件开放（2 周）**——补效果原子 + JSON 导入器，玩家事件 mod 落地（不动旧事件，风险可控）
3. **P2 决策/科技（1~2 周）**——两类内容出代码
4. **P3/P4 接线与交付**——外交动作接线 + 文档示例

建议从 P0 开工：数据外置层是一切的前提，做完当天就有可演示的"玩家改本体"效果。
