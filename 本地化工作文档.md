# 本地化工作文档

> 适用范围：事件系统全部文案（标题/描述/选项/结果）+ 脚本动态文案。
> 最后更新：2026-08。配套工具见 `tools/`，引擎侧实现见 `数据脚本/core/event_text.gd`、
> `数据脚本/factory/event_def_builder.gd`、`数据脚本/systems/event_engine.gd`。

---

## 1. 总览：一条文案的生命周期

```
资产/本地化/events_zh_CN.csv          ← 唯一文案源（键 → 各语言列）
        │  Godot CSV 导入器
        ▼
events_zh_CN.zh_CN.translation        ← 生成的翻译资源（项目设置已注册）
        │
事件触发 → GameManager 取 EventDef（字段里只有键，没有文案）
        │
        ▼
显示前最后一刻：
  EventText.t(键)  ──► TranslationServer 查当前语言 ──► 译文
  （prepare/execute 里可再 .format() 填 {0}{1} 变量）
        │
        ▼
BbcTooltip.event_text_to_bbcode()     ← 颜色片段回填（按原版编号查 JSON）
        │
        ▼
_format_event_text()                  ← 缩进/段距排版；[原样]块跳过
        │
        ▼
RichTextLabel 渲染
```

**核心原则**：代码与 META 里永远只有键；译文在渲染前一刻才取。
绝不要把 `tr()` / `EventText.t()` 的结果写回共享的 EventDef 字段——EventDef 是
全局单例，写回会导致切语言后显示陈旧文本。

---

## 2. 文件地图

| 文件 | 作用 |
|---|---|
| `资产/本地化/events_zh_CN.csv` | 全部事件文案。第一列 `keys`，之后每列一个语言（`zh_CN`…） |
| `场景/事件界面/events/` | 已清空（.tres 已删除）。目录保留作扫描兜底 |
| `数据脚本/事件效果/*.gd` | 515 个合并式事件：META 在文件尾部 + execute 效果逻辑 |
| `数据脚本/事件定义/*.gd` | 72 个独立定义：纯声明式事件 / 共享脚本事件的 META |
| `数据脚本/core/event_text.gd` | `t()` 翻译、`fmt()` 翻译+填变量、键拼接约定 |
| `数据脚本/factory/event_def_builder.gd` | 运行时把 META 字典构建成 EventDef 对象树 |
| `project.godot` | `[internationalization]`：translations 列表 + fallback=zh_CN |

当前规模：587 个事件，8847 个键（结构槽 ~2655 + 脚本动态文案 4287 + 其他）。

---

## 3. 键名约定（自动派生，不要手拼）

| 键 | 来源 |
|---|---|
| `event.<id>.title` / `.desc` | META 的 id；builder 自动生成 |
| `event.<id>.option_<N>.text` | 第 N 个选项（0 起），builder 恒生成 |
| `event.<id>.option_<N>.disabled` | 该选项有门槛提示时才存在 |
| `event.<id>.option_<N>.result` | 该选项有静态结果文本时才存在 |
| `event.<id>.option_<N>.result_title` | 结果标题（罕见） |
| `event.script.<文件名>.c<N>` | 效果脚本里的 TXT_* 文本常量（值即键） |
| `event.script.<文件名>.i<N>` | 脚本内联 `context["result_text"] = tr("…")` |

规则：
- **META 不含任何文案**。控制存在性的标志：`notitle` / `nodesc`（事件级）、
  `notext` / `disabled` / `result` / `result_title`（选项级）。
- 键一律由 builder 从 id+序号派生；手写事件也照抄此约定，别自创格式。
- 一个选项若没有任何静态文本，对应行就不该出现在 CSV。

---

## 4. 写作规范

### 4.1 变量占位

CSV 里写 `{0}{1}…`（或命名键 `{name}`），脚本运行时填充：

```gdscript
context["result_text"] = EventText.fmt("event.xxx.desc", {"0": 姓, "1": 名})
# 等价于 t(key).format(vars, "{_}")
```

领袖名等动态信息只在 prepare()/execute() 里拼——CSV 是纯静态文本，
不会自己取世界状态。

### 4.2 样式标签支持矩阵

| 显示槽位 | 控件 | BBCode |
|---|---|---|
| 事件描述、结果文本 | RichTextLabel | ✅ `[b] [i] [u] [s] [color] [code] [center]` |
| 事件标题 | Label | ❌ 会显示原始标签 |
| 选项按钮文字 | Label | ❌ 同上 |

- `[b]/[i]` 由 `事件.gd::_setup_style_fonts()` 用 FontVariation 合成
  （embolden 0.6 / 剪切 -0.22）；换真实字体文件时改这一处。
- 颜色建议用常量风格（参考测试事件的 C_GOLD 等）保持全游戏一致。

### 4.3 [原样] 块

```text
正文段落……

[原样]
北国风光，
千里冰封。
[/原样]

正文继续……
```

块内逐字渲染：不加首行缩进、不产生段距、空行保留、无视玩家排版设置。
标记在任何设置组合下都会被剥除；未闭合视为延伸到文末。
适用：诗歌、信件、清单等需要固定分行的内容。

### 4.4 数值语义陷阱

预算 / 党内支持 / 民众支持 / 军力内部按 **×10** 存储，展示前 ÷10
（参考测试事件 `_disp()`）。政治家的 loyalty 同为 ×10 且语义是
「政客对领袖的忠诚」，不要安在领袖自己头上。

### 4.5 颜色片段（人名/专名上色）

颜色不在文本里——移植时已剥除，显示时由
`BbcTooltip.restore_event_colors()` 按 **原版事件编号** 查
`资产/数据/event_color_fragments*.json`，用中文原句做子串匹配回填。
因此：改中文文案时若动了含色句子，JSON 里的 plain 片段会失配（该句掉色）。
未来加英文列时此机制对译文无效，需按语言维护片段表或改按键定位（待办）。

---

## 5. 工作流

### 5.1 新增一个事件

1. 建 `数据脚本/事件效果/event_xxx.gd`（或纯声明放 `事件定义/`），
   `extends EventScriptBase`，尾部按约定写 `const META := {…}`；
   动态内容实现 `prepare(event_def, world)` / `execute(context)`。
2. 手动触发的测试事件：META 省略 `trigger` 即可，控制台 `event <id>` 直发。
3. 把所有文案以 `event.<id>.*` 行追加进 CSV（多语言就多加几列的对应格）。
4. 跑审计（见 §6）确认零缺失，进游戏验证。

### 5.2 改文案 / 加语言

- 改中文：直接编辑 CSV 对应格，保存后 Godot 会自动重导入 translation。
- 加新语言：CSV 加一列（表头用合法 locale 如 `en`）→ 项目设置
  国际化→翻译 里追加自动生成的 `.translation` 文件 → 重启编辑器。
  未命中的键会回退 fallback_locale（当前 zh_CN）。

### 5.3 排查问题

| 现象 | 检查 |
|---|---|
| 界面显示原始键名 | 该键在 CSV 中缺失 → 跑审计工具定位 |
| 样式标签原样露出 | 该槽位是 Label（标题/选项按钮），移到描述/结果里 |
| 粗体斜体没变化 | `_setup_style_fonts` 是否生效；字体变体被覆盖？ |
| 人名没颜色 | 文本与 JSON plain 片段失配（§4.5） |
| 数值大 10 倍 | 忘了 ÷10（§4.4） |

---

## 6. 工具箱（tools/）

| 工具 | 用途 |
|---|---|
| `audit_event_keys.gd` | **日常主用**。全量构建 META + 扫描脚本键，对照 CSV 报缺失。提交前必跑 |
| `test_format_layout.gd` | 排版纯函数回归（缩进/段距/[原样]/标记剥除） |
| `test_color_dynamic.gd` | 颜色还原 + 变量填充链路探针 |
| `migrate_events_to_gd.gd` | 【历史】.tres → META 一次性迁移（已完成） |
| `extract_script_texts.gd` | 【历史】脚本硬编码文案提取 |
| `repair_script_keys.gd` / `fix_texts_from_snapshot.gd` | 【历史】迁移期撞号修复 |
| `clean_placeholder_slots.gd` / `patch_meta_flags.gd` | 【历史】占位符清理 |
| `migration_manifest.json` | stem→定义文件映射，audit/verifier 依赖 |

运行方式统一为：
`Godot --headless --path . --script res://tools/<名字>.gd`

---

## 7. 引擎接入点速查（改引擎时看）

- `event_engine.gd`：扫描源 = `scan_script_directories`（两个 .gd 目录），
  `scan_legacy_tres=false`（.tres 已删，勿开）；同 id 先到先得。
- 显示点必须经 `EventText.t()`：`事件.gd` 三处 + 引擎的通知/getter/结果解析。
- `display_script` 指向自身即可让 merged 单文件事件获得 prepare 钩子。
- 新增 ExprNode/EffectNode 枚举只能尾追加（枚举整数兼容），META 用枚举名
  字符串（如 `"add_resource"`），builder 经反射建表，无需登记。
