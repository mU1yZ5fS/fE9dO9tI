# 创意工坊化（Mod 系统）可行性分析

> 目标：像钢铁雄心4（HOI4）那样，玩家用简单文档即可扩展国家、事件、科技、政治家、决议等。
> 本分析基于对当前代码库的实测（各内容类型的数据化程度、效果层实现方式），不参考任何既有文档。

---

## 1. HOI4 创意工坊的本质（三个支柱）

1. **内容即数据**：事件/国策树/决议/科技/顾问/国家全部是文本声明（txt），引擎只负责"解析 + 执行"——内容与引擎完全分离。
2. **加载管线**：mod 目录 → 解析 → 注册表合并（覆盖/追加）→ 错误校验，坏 mod 不影响本体。
3. **分发渠道**：Steam Workshop（订阅 → 下载 → 启用），本质只是 mod 文件的下载/管理通道。

## 2. 本项目内容数据化程度盘点（实测数据）

| 内容 | 现状实现 | Mod 友好度 |
|---|---|---|
| 政治家 | `.tres` 完全数据化（PoliticianData + 肖像纹理） | ★★★ 直接可用 |
| 修正 | `.tres` 数据化（ModifierDef + 图标） | ★★★ 直接可用 |
| 战争 | `.tres` 数据化（WarDef） | ★★★ 直接可用 |
| 事件定义 | `.tres` 数据化（EventDef + ExprNode/EffectNode DSL） | ★★☆ 定义可 mod，**效果层不行** |
| 事件效果 | **583 个事件中 581 个含 CUSTOM_SCRIPT（99.7%）；`type=18` 脚本效果出现 1808 次**，声明式效果（加资源/flag/联盟/事件链等 20+ 种）合计约 1400 次但多为辅助 | ★ 玩家写事件 = 写 GDScript |
| 决策 | `DecisionCatalog.build()` **代码内嵌**（_build_v1/v2 逐条创建） | ★ 不可 mod |
| 科技 | `tech_state.gd` const 数组 + `科研.gd` const 文案 + **tscn 手摆条目** | ★ 不可 mod |
| 国家 | `world_factory.gd` 1,641 行**内嵌 const** | ★ 不可 mod |
| 外交动作 | `外交互动_批1~6` 232 个 `_def_N` 代码函数 | ☆ 不可 mod |

**结论：项目已经拥有一半的 mod 基础**（政治家/修正/战争/事件定义都是资源化），但效果层、决策、科技、国家、外交动作仍是代码——玩家视角"写文档就能扩展"目前只有政治家/修正/战争成立。

## 3. 可行性结论：可行，分三档

| 档位 | 内容 | 工作量 | 说明 |
|---|---|---|---|
| **A 档** | ModLoader 框架 + 政治家/修正/战争 mod | **1~2 天** | 已有 .tres，只需目录扫描 + 注册表合并，立即可玩 |
| **B 档** | 事件 mod（JSON 导入器 + 效果层去脚本化）+ 决策数据化 | **2~3 周** | 最大头是效果层：CUSTOM_SCRIPT 99.7% → <20% |
| **C 档** | 科技/国家/外交动作数据化 | **1~2 个月** | 内容出代码的大改造（与重构路线重合） |

## 4. 架构设计

### 4.1 Mod 目录约定（仿 HOI4，解压即用）

```
mods/我的MOD/mod.json              ← 元数据：名称/作者/版本/依赖/启用顺序
mods/我的MOD/events/*.json         ← 事件（JSON DSL）
mods/我的MOD/decisions/*.json      ← 决议
mods/我的MOD/techs/*.json          ← 科技
mods/我的MOD/politicians/*.tres    ← 政治家（复用现有格式）
mods/我的MOD/countries/*.json      ← 国家
```

加载顺序：本体 → 各 mod（按启用顺序）→ 同 id **mod 覆盖本体、后启用覆盖先启用**。

### 4.2 事件 mod 格式（玩家写 JSON，非 Godot 格式）

```json
{
  "id": "my_event_001",
  "title": "示例事件",
  "description": "这是玩家 mod 的事件。",
  "trigger_conditions": [
    { "type": "resource_at_least", "key": "budget", "value": 100 }
  ],
  "options": [
    {
      "text": "接受",
      "effects": [
        { "type": "add_resource", "key": "budget", "value": -10 },
        { "type": "set_flag", "key": "my_mod_flag" }
      ]
    }
  ]
}
```

实现路径：**JSON → ExprNode/EffectNode 树 → EventDef**——节点结构已存在（@export 字段齐全），只缺一个"JSON 到节点树"的导入器（event_factory.gd 已有反向的生成器，可对照）。

### 4.3 效果层去脚本化（B 档最大工程）

现状 99.7% 事件效果走脚本，**玩家不可能为每个事件写 GDScript**。必须把高频脚本效果补成声明式：

- **已有** 20+ 种：资源增减/赋值、联盟、国家变量、帝国关系/实力、派系支持、修正开关、flag、事件链、开战
- **缺口**（脚本高发区，需新增原子效果）：
  - 政治家操作（忠诚增减、入职/离职、移除槽位）——如 event_003 覆写政客
  - 逐国/逐人循环（对指定列表国家批量施加）
  - 动态文案（{0}{1} 领袖姓名占位、按条件选文案）——显示层钩子
  - 条件效果块（if/elif 嵌套）
- **保留脚本钩子**作为高级能力（mod 允许附带 .gd，但不再是唯一路径）
- **最大风险**：581 个既有脚本事件的行为不能漂移——去脚本化必须逐事件回归（这是此前"效果 DSL 被架空"的债，还债时顺手打通 mod）

### 4.4 ModLoader（autoload）

```
ModLoader (autoload)
├── 扫描 mods/ → 读 mod.json → 内容清单
├── 注册表合并（按类型分发）：
│   EventEngine._events / PoliticianPool / ModifierCatalog / WarCatalog /
│   DecisionCatalog / TechCatalog / 国家表
├── 冲突策略：id 前缀规范（mod 名），同 id 后启用者胜
├── 校验与日志：坏文件 → 收集错误（启动界面展示或日志），本体不受影响
└── 存档兼容：completed_event_ids 按 id 存；卸载 mod 后未知 id 无害 ✓
```

### 4.5 分发

- **先行**：本地 mod 目录（解压即用，符合现状：游戏未接 Steam）
- **Steam 创意工坊**：需 Steam 发行版 + Workshop 集成（Godot 可通过 Steam 插件实现上传/订阅/下载）——发行决策，非技术障碍

## 5. 推荐路线

| 阶段 | 内容 | 产出 |
|---|---|---|
| 1 | ModLoader 骨架 + 政治家/修正/战争 mod 接入 | 第一个可用 mod（1 天内） |
| 2 | 事件 JSON 导入器 + 声明式效果补全（脚本占比 99.7%→<20%） | 事件 mod 可用 |
| 3 | 决策数据化（DecisionCatalog 代码 → .tres/JSON） | 决议 mod 可用 |
| 4 | 科技数据化 + 条目 UI 数据驱动（关系线已自动化，条目待数据化） | 科技 mod 可用 |
| 5 | 国家/外交动作数据化（C 档，量大） | 国家 mod 可用 |
| 6 | 示例 mod 包 + 玩家文档 + 校验器 UI | 交付 |

阶段 1-3 与"内容出代码"重构路线高度重合——**做 mod 系统 = 以玩家视角驱动同一场重构**，不是额外工作。

## 6. 风险清单

| 风险 | 等级 | 缓解 |
|---|---|---|
| 效果去脚本化导致 581 个事件行为漂移 | 高 | 逐事件回归 + 快照比对；脚本钩子保留兜底 |
| 复杂事件表达力不足（动态文案/逐国循环） | 中 | 新增原子效果 + 高级 mod 允许 .gd |
| mod 间 id 冲突/卸载后存档残留 | 中 | id 前缀规范 + 存档宽容（未知 id 忽略） |
| 玩家文档质量 | 中 | 示例 mod + 校验器报错信息友好化 |
| 性能 | 低 | 解析在加载期一次性完成，运行时零开销 |

## 7. 一句话结论

**能实现，且项目已有 40% 地基**（政治家/修正/战争/事件定义均已资源化）。先做"本地 mod 目录 + 加载器"（1-2 天打通 A 档），随后借"效果层去脚本化"的还债过程打通事件 mod（B 档 2-3 周），科技/国家/外交动作属于长期改造（C 档）——Steam 创意工坊本身只是发行侧集成，技术路径无阻碍。
